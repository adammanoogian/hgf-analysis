"""Stage 04a — MLE / VB-Laplace parallel alternative.

Reserved; consolidated CLI not yet written. See plan 26-04 task 1 for current
invocation paths. Currently the VB-Laplace path is invoked via
``scripts/03_pre_analysis/08_smoke_patrl_foundation.py`` and library function
``prl_hgf.fitting.fit_vb_laplace_patrl``; consolidating into a dedicated
``fit.py --method mle`` CLI is a future refactor (not this phase).
"""

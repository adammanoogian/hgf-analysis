---
phase: 23-export-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/23-export-template/TEMPLATE_AUDIT.md
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "TEMPLATE_AUDIT.md exists under .planning/phases/23-export-template/ BEFORE any figures/hgf_viewer.html or src/prl_hgf/viz/export.py lines are written (P11 procedural guard)"
    - "Audit classifies every occurrence of `balanced`, `reward_blunted`, `threat_hypervigilant`, `both_disrupted`, `Klaassen`, `bradycardia`, `C.dhr`, `PHENOS`, `shock_mag`, `reward_mag`, `response_patrl` in figures/patrl_hgf_model.html as R (remove) | I (inject) | G (conditional-guard)"
    - "Audit explicitly records that `Klaassen` has 0 hits (ROADMAP listed it but it is absent from the seed; research §1)"
    - "Audit records Google Fonts presence: `grep -nE 'googleapis|@import.*font' figures/patrl_hgf_model.html` returns >=2 lines (preconnect + CSS stylesheet link) — both classified R for plan 23-02 excision (EXPORT-05 clause (b) system-font fallback)"
    - "pyproject.toml [project.dependencies] gains `jinja2>=3.1,<4` (making the Jinja2 transitive dep explicit)"
    - "pyproject.toml [project.optional-dependencies.dev] gains `pytest-snapshot>=0.9.0` and `beautifulsoup4>=4.12`"
    - "pyproject.toml [[tool.mypy.overrides]] module list gains `pytest_snapshot.*` and `bs4.*` so test-file imports do not trigger missing-stubs errors"
    - "ds_env has all three packages importable at phase 23-01 close — verified empirically by `conda run -n ds_env python -c \"import jinja2, bs4, pytest_snapshot\"` (P15 guard)"
    - "Parallel-stack invariant holds at plan close: `git diff --stat` on src/prl_hgf/{env,models,fitting,analysis,gui,power,simulation}/ shows zero lines"
  artifacts:
    - path: ".planning/phases/23-export-template/TEMPLATE_AUDIT.md"
      provides: "Full grep catalog + R/I/G classification of PAT-RL hardcoded surface in the seed template"
      min_lines: 120
      contains: "PHENOS"
    - path: "pyproject.toml"
      provides: "jinja2 runtime dep + pytest-snapshot/beautifulsoup4 dev deps + mypy overrides"
      min_lines: 100
      contains: "jinja2>=3.1,<4"
  key_links:
    - from: ".planning/phases/23-export-template/TEMPLATE_AUDIT.md"
      to: "figures/patrl_hgf_model.html"
      via: "grep of 11 audit terms + Google Fonts + classification table"
      pattern: "PHENOS|bradycardia|C\\.dhr|response_patrl|googleapis"
    - from: "pyproject.toml [project.dependencies]"
      to: "jinja2 (runtime)"
      via: "Jinja2 Environment(loader=BaseLoader()) used by export.py in plan 23-03"
      pattern: "jinja2>=3\\.1"
    - from: "pyproject.toml [project.optional-dependencies.dev]"
      to: "pytest_snapshot + bs4 (dev only)"
      via: "tests/test_viz_export.py in plan 23-04 imports both; bs4 must NOT leak into runtime"
      pattern: "pytest-snapshot|beautifulsoup4"
---

<objective>
Commit TEMPLATE_AUDIT.md (the P11 procedural guard) and land the DEPS-01
dependency bump BEFORE any template-promotion or export.py code is
written. This plan is pure discovery + config; no Python code, no HTML
changes.

Purpose: Research finding #7 — "Audit-first is procedurally critical;
template promotion before audit inverts the guard." Plan 1 exists to
make that order enforceable at the plan graph level (plans 23-02/03/04
all depend on 23-01). Also: pytest-snapshot is NOT installed in ds_env
(research §10.6, finding #2) — plan 1 is the only place DEPS-01 can
land before the test plan needs it.

Output: `.planning/phases/23-export-template/TEMPLATE_AUDIT.md`,
updated `pyproject.toml`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-export-template/23-RESEARCH.md
@.planning/phases/22-inspector-roles-schema-scaffold/22-04-SUMMARY.md
@figures/patrl_hgf_model.html
@pyproject.toml

# CRITICAL DISCREPANCIES (ROADMAP vs RESEARCH):
# - ROADMAP success criterion 1 lists `Klaassen` as an audit term — it has
#   0 hits in the seed (research §1 pre-planning grep verified). The audit
#   MUST still grep for it and record the 0-hit result explicitly; do NOT
#   silently drop it from the term list.
# - Research finding #2: pytest-snapshot is ACTUALLY missing from ds_env.
#   Verified via ModuleNotFoundError. DEPS-01 is real install work — not
#   a rubber-stamp. The `python -c "import pytest_snapshot"` check in task
#   2's P15 verify block is load-bearing, not ceremonial.
# - Research §10.7: mypy will error on `pytest_snapshot` imports in tests
#   unless added to [[tool.mypy.overrides]] ignore_missing_imports list.
#   Same pattern that already exists for jaxopt, numpyro, etc.
# - EXPORT-05 clause (b): seed template imports Source Sans 3 + STIX Two
#   Text from fonts.googleapis.com (verified 2026-04-24: 2 hits at seed
#   lines 7 and 9). Audit MUST classify both as R — system-font fallback
#   only in v1.3 (Google Fonts inlining deferred to v1.4 POLISH-05).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit TEMPLATE_AUDIT.md (P11 guard — before any other phase 23 work)</name>
  <files>.planning/phases/23-export-template/TEMPLATE_AUDIT.md</files>
  <action>
Create `.planning/phases/23-export-template/TEMPLATE_AUDIT.md`. This is
the P11 procedural guard artifact. It MUST be committed before any
`figures/hgf_viewer.html` or `src/prl_hgf/viz/export.py` lines exist.

STRUCTURE (match the preview in research §1):

1. **Header block** — date (2026-04-24), source file
   (`figures/patrl_hgf_model.html`, 676 lines SHA-short),
   classification legend (R/I/G).

2. **Grep output section** — run each audit term against the seed and
   record the line numbers. Use this exact bash one-liner pattern per term:
   ```bash
   grep -n 'balanced' figures/patrl_hgf_model.html
   ```
   Record the ROADMAP-mandated 11 terms: `balanced`, `reward_blunted`,
   `threat_hypervigilant`, `both_disrupted`, `Klaassen`, `bradycardia`,
   `C.dhr`, `PHENOS`, `shock_mag`, `reward_mag`, `response_patrl`.

   For `Klaassen`: record `(0 hits — ROADMAP listed this term but it is
   absent from the seed template)`. Do NOT quietly drop it.

   ADDITIONAL grep — Google Fonts scan (EXPORT-05 clause (b)):
   ```bash
   grep -nE 'googleapis|@import.*font|fonts\.googleapis\.com' figures/patrl_hgf_model.html
   ```
   Expected: 2 hits (lines 7 `<link rel="preconnect" ...googleapis...>`
   and 9 `<link href="...fonts.googleapis.com/css2?family=Source+Sans+3...
   STIX+Two+Text..." rel="stylesheet">`). Record BOTH hits as R (classify
   for excision in plan 23-02) — v1.3 uses system-font fallback only.

3. **Classification tables** — one table per region, matching research
   §1 layout:
   - Title + metadata (line 6)
   - Google Fonts `<link>` block (lines 7-9) — ALL R (system-font fallback)
   - Phenotype block (lines 45-87, 125-158, 231-244)
   - ANS / SNS / PNS framing in INFO dict (lines 91-112)
   - Response model controls (lines 122, 131-132, 216-229, 343-357)
   - DHR node + edges (lines 40, 42, 576-607, 540-550, 251)
   - EV block (lines 302, 534-562)
   - HGF level radio buttons (line 121 P10 guard — this is `I` not `R`)
   - Literal citations / bradycardia (lines 110, 111)
   - Notation sidebar (lines 247-275, keep with 2 removals)
   - Helper SVG geometry (lines 115-117 — KEEP)

   Each table row: `| Lines | Content | Classification | Rationale |`.

4. **Summary section** — total unique occurrences to remove (~34 per
   research §1 + 2 Google Fonts `<link>` tags), total injection slots
   (per line 121 P10 + line 6 title + per-node tooltip fields in INFO
   dict), total KEEP regions (hexPts, diamPts, retract, is3/isMM
   branching, notation sidebar with minor edits, level band rectangles).

5. **Open questions for plan 23-02** (if any — e.g., "INFO dict tooltip
   bodies: strip entirely or placeholder with per-node labels from
   NetworkSpec? Recommendation: strip for v1.3 since NetworkSpec has no
   label field; Phase 24 may add").

6. **P10 guard verification commands** (at bottom — to be run at plan
   23-02 close):
   ```bash
   grep -n 'useState("3level")' figures/hgf_viewer.html     # must be 0
   grep -n 'NETWORK_SPEC\\.nodes' figures/hgf_viewer.html   # must be >=1
   grep -cE 'googleapis|@import.*font' figures/hgf_viewer.html  # must be 0
   ```

COMMIT MESSAGE: `docs(23-01): commit TEMPLATE_AUDIT.md (P11 guard before any template-promotion or export.py work)`.
  </action>
  <verify>
- `wc -l .planning/phases/23-export-template/TEMPLATE_AUDIT.md` reports >=120 lines.
- `grep -c 'PHENOS' .planning/phases/23-export-template/TEMPLATE_AUDIT.md` reports >=3 (appears in grep-output section + classification table + summary).
- All 11 ROADMAP grep terms appear as section headers or table entries (including `Klaassen` recorded as 0-hit).
- `grep -n "(0 hits" .planning/phases/23-export-template/TEMPLATE_AUDIT.md` includes the `Klaassen` zero-hit record.
- `grep -cE 'googleapis|fonts\\.googleapis' .planning/phases/23-export-template/TEMPLATE_AUDIT.md` reports >=2 (Google Fonts hits recorded).
- `git log --stat -1 -- .planning/phases/23-export-template/TEMPLATE_AUDIT.md` shows this as the first phase 23 commit touching any file other than RESEARCH/CONTEXT.
- **P11 guard check**: `ls figures/hgf_viewer.html src/prl_hgf/viz/export.py 2>/dev/null` returns "No such file or directory" for BOTH paths at the time of this commit (the audit precedes the template).
  </verify>
  <done>
TEMPLATE_AUDIT.md committed under `.planning/phases/23-export-template/`.
Classifies every hit of all 11 ROADMAP audit terms + 2 Google Fonts
`<link>` tags. `Klaassen` 0-hit recorded explicitly. P10 guard
verification commands listed at the bottom for plan 23-02 to run. No
`figures/hgf_viewer.html` or `src/prl_hgf/viz/export.py` files exist yet
(P11 ordering enforced).
  </done>
</task>

<task type="auto">
  <name>Task 2: pyproject.toml — add jinja2 runtime dep + pytest-snapshot/bs4 dev deps + mypy overrides + P15 ds_env import verification</name>
  <files>pyproject.toml</files>
  <action>
Edit `pyproject.toml` per DEPS-01 (ROADMAP success criterion 5), then
install the new packages in ds_env and verify imports (P15 guard —
research finding #2 confirmed pytest-snapshot is ACTUALLY missing
from ds_env; this is real install work, not rubber-stamp).

**Edit 1 — `[project.dependencies]`** (currently lines 11-32):
Insert `jinja2>=3.1,<4` into the list. Recommended position: after
`pyyaml>=6.0` (alphabetical-ish; matches the rough grouping style — yaml
and jinja are both text/template tools). The exact final dependency list
must remain alphabetically close but NOT strictly re-sorted (preserves
blame history for the other 21 entries). Add one new line:
```toml
    "jinja2>=3.1,<4",
```

**Edit 2 — `[project.optional-dependencies].dev`** (currently lines 37-43):
Replace the existing dev list with:
```toml
dev = [
    "ruff>=0.4",
    "mypy>=1.10",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-snapshot>=0.9.0",
    "beautifulsoup4>=4.12",
]
```

**Edit 3 — `[[tool.mypy.overrides]]` module list** (currently lines
76-88): Append `"pytest_snapshot.*"` and `"bs4.*"` to the module list
(inside the existing entry — do NOT create a second overrides block).
Final state of that module list should include these two entries
alongside the existing `jaxopt.*`, `numpyro.*`, etc.

Do NOT reformat anything else in the file. Do NOT touch other sections.

**Install + P15 import verification (ds_env):**

```bash
conda run -n ds_env pip install "pytest-snapshot>=0.9.0" "beautifulsoup4>=4.12"
```
(jinja2 is already present at 3.1.6; direct pip install avoids a full
`pip install -e .` reinstall.)

Then the P15 guard runs in the `<verify>` block below.

COMMIT MESSAGE: `chore(23-01): add jinja2 runtime dep + pytest-snapshot/beautifulsoup4 dev deps (DEPS-01) + verify ds_env importable`.
Include the three version strings printed by the P15 check in the commit
message body for a traceable baseline.
  </action>
  <verify>
- `grep -n '"jinja2>=3.1,<4"' pyproject.toml` returns 1 match inside the `[project.dependencies]` list.
- `grep -n '"pytest-snapshot>=0.9.0"' pyproject.toml` returns 1 match inside `[project.optional-dependencies].dev`.
- `grep -n '"beautifulsoup4>=4.12"' pyproject.toml` returns 1 match inside `[project.optional-dependencies].dev`.
- `grep -nE '"(pytest_snapshot|bs4)\\.\\*"' pyproject.toml` returns 2 matches inside `[[tool.mypy.overrides]]`.
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` succeeds (still valid TOML).
- **P15 import verification (ds_env)**:
  ```bash
  conda run -n ds_env python -c "
  import jinja2, bs4, pytest_snapshot
  print('jinja2:', jinja2.__version__)
  print('bs4:', bs4.__version__)
  print('pytest_snapshot:', pytest_snapshot.__version__)
  "
  ```
  Exits 0 and prints three non-empty version strings. Expected baseline
  (2026-04-24 ds_env): jinja2 3.1.6, bs4 4.13.4, pytest_snapshot >=0.9.0.
- **pytest-snapshot fixture auto-registration smoke-check**:
  ```bash
  conda run -n ds_env pytest --fixtures tests/test_models.py 2>&1 | grep -i snapshot
  ```
  Returns at least one line mentioning the `snapshot` fixture.
- **API-surface smoke-check**:
  ```bash
  conda run -n ds_env python -c "from pytest_snapshot.plugin import Snapshot; print(Snapshot)"
  ```
  Prints the class without ImportError.
- `git diff --stat src/prl_hgf/env/ src/prl_hgf/models/ src/prl_hgf/fitting/ src/prl_hgf/analysis/ src/prl_hgf/gui/ src/prl_hgf/power/ src/prl_hgf/simulation/` reports zero lines changed (parallel-stack invariant).
  </verify>
  <done>
pyproject.toml updated with exactly three edits: jinja2 added to
runtime deps; pytest-snapshot + beautifulsoup4 added to dev deps;
pytest_snapshot.* + bs4.* added to mypy ignore_missing_imports list.
No other sections touched. TOML still parses. ds_env has all three
packages importable (P15 guard satisfied); exact versions captured in
the commit message body. Plan 23-04 can import `pytest_snapshot` and
`bs4` without any further `pip install` step.
  </done>
</task>

</tasks>

<verification>
**Plan 23-01 close checks (run before commit):**

1. P11 guard — at the moment of the TEMPLATE_AUDIT.md commit (task 1
   commit), neither `figures/hgf_viewer.html` nor `src/prl_hgf/viz/export.py`
   exists on disk:
   ```bash
   ls figures/hgf_viewer.html src/prl_hgf/viz/export.py 2>&1 | grep -c "No such file"
   # expect: 2
   ```

2. DEPS-01 verification:
   ```bash
   grep -n 'jinja2>=3.1,<4\|pytest-snapshot>=0.9.0\|beautifulsoup4>=4.12' pyproject.toml
   # expect: 3 matches
   ```

3. mypy overrides:
   ```bash
   grep -nE '"(pytest_snapshot|bs4)\\.\\*"' pyproject.toml
   # expect: 2 matches
   ```

4. P15 import check (ds_env):
   ```bash
   conda run -n ds_env python -c "import jinja2, bs4, pytest_snapshot; print('all three import ok')"
   # expect: "all three import ok"
   ```

5. Parallel-stack invariant:
   ```bash
   git diff --stat src/prl_hgf/env/ src/prl_hgf/models/ src/prl_hgf/fitting/ src/prl_hgf/analysis/ src/prl_hgf/gui/ src/prl_hgf/power/ src/prl_hgf/simulation/
   # expect: empty (plan 23-01 does not touch the parallel stacks)
   ```
</verification>

<success_criteria>
- TEMPLATE_AUDIT.md committed FIRST (P11 procedural guard active for
  the rest of phase 23); classifies every hit of 11 ROADMAP audit terms
  including `Klaassen` (0 hits, recorded) + 2 Google Fonts `<link>` tags.
- pyproject.toml gains `jinja2>=3.1,<4` runtime dep, `pytest-snapshot>=0.9.0`
  and `beautifulsoup4>=4.12` dev deps, `pytest_snapshot.*` and `bs4.*` mypy
  overrides.
- ds_env verified to import all three packages via P15 guard.
- Zero lines changed in any non-viz src/prl_hgf/ subpackage.
- Ready state for plan 23-02 (template promotion can begin with the audit
  in hand) and plan 23-03 (export.py can `import jinja2`) and plan 23-04
  (test_viz_export.py can `from bs4 import BeautifulSoup` + use `snapshot`
  fixture).
</success_criteria>

<output>
After completion, create
`.planning/phases/23-export-template/23-01-SUMMARY.md` using the
execute-plan summary template. Include:
- The `pyproject.toml` diff (5-line patch summary).
- The three version strings printed by P15 guard (jinja2/bs4/pytest_snapshot).
- The grep counts of audit terms (11 terms recorded; ~34 unique
  occurrences classified) + 2 Google Fonts `<link>` tags recorded.
- Confirmation that `figures/hgf_viewer.html` and `src/prl_hgf/viz/export.py`
  still do not exist at phase close (P11 ordering preserved for plan 23-02).
</output>
</output>

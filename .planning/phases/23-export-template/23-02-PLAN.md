---
phase: 23-export-template
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - figures/hgf_viewer.html
  - .planning/REQUIREMENTS.md
autonomous: false

must_haves:
  truths:
    - "figures/hgf_viewer.html exists as a NEW file (copy of figures/patrl_hgf_model.html promoted to generic), with figures/patrl_hgf_model.html preserved unchanged"
    - "All (R)-classified regions from TEMPLATE_AUDIT.md are excised from hgf_viewer.html (PHENOS array, PHENOS sidebar block, DHR node + edges, EV computation block, response-model radio buttons, ANS/SNS/PNS tooltip bodies, Google Fonts <link> preconnect + stylesheet)"
    - "Line 121 `const [hgfLevel, setHgfLevel] = useState(\"3level\");` is REPLACED with derivation from NETWORK_SPEC via `const INITIAL_HGF_LEVEL = (() => { const maxL = NETWORK_SPEC.nodes.reduce((m,n)=>Math.max(m,n.level),0); if (maxL>=3) return \"3level\"; if (NETWORK_SPEC.input_idxs.length>1) return \"2level_mm\"; return \"2level\"; })()` + `useState(INITIAL_HGF_LEVEL)` (P10 guard, research §6)"
    - "Two injection marker <script type=\"application/json\"> tags present: id=\"viz-network-spec\" (carrying a valid default JSON blob — the PAT-RL 3-level NetworkSpec payload from research finding #6, Strategy B) and id=\"viz-posterior-summary\" (empty body for pre-fit, populated in Phase 24)"
    - "Module-scope top-of-script-babel block parses both injection tags via `document.getElementById(...).textContent` + `JSON.parse` (research §6 canonical pattern); posterior-summary block defensively handles empty body"
    - "Jinja2 `{% if react_inlined %}` / `{% else %}` block wraps the three CDN `<script src=...>` tags (React, React-DOM, Babel) — version-pinned to 18.2.0 / 18.2.0 / 7.23.2 (EXPORT-05)"
    - "Bare-open property verified: opening figures/hgf_viewer.html in a browser without running render_viewer_html renders the PAT-RL 3-level topology from the baked-in default JSON (research §2 Strategy B, research §10.2 mitigation)"
    - "P10 grep guard satisfied: `grep 'useState(\"3level\")' figures/hgf_viewer.html` returns 0 matches; `grep 'NETWORK_SPEC' figures/hgf_viewer.html` returns >=1 match"
    - "Audit grep guard satisfied at plan close: `grep -cE 'balanced|reward_blunted|threat_hypervigilant|both_disrupted|PHENOS|C\\.dhr|response_patrl|shock_mag|reward_mag|bradycardia' figures/hgf_viewer.html` returns 0 (all R-classified terms excised; grep that still hits would prove incomplete template promotion)"
    - "EXPORT-05 clause (b) — system fonts only (Google Fonts excised): `grep -cE 'googleapis|@import.*font|fonts\\.googleapis\\.com' figures/hgf_viewer.html` returns 0 (standing guard for v1.3; inlining deferred to v1.4 POLISH-05)"
    - "TEMPLATE-08 multimodal 2-level rendering verified interactively in task 3 checkpoint: clicking multimodal radio shows two input diamonds coupling to one shared belief circle (no dead space)"
    - "TEMPLATE-09 is PARTIAL in Phase 23 — response-model section renders a generic hardcoded placeholder (`Decision equation: rendered per response model (injected)`) below a horizontal separator; spec-derived rendering from a NetworkSpec.response_model field is DEFERRED to Phase 24 (requires schema extension). REQUIREMENTS.md traceability row updated from `Pending` to `Partial — Phase 24` to match reality"
    - "Parallel-stack invariant holds: `git diff --stat src/prl_hgf/{env,models,fitting,analysis,gui,power,simulation}/` reports zero lines"
  artifacts:
    - path: "figures/hgf_viewer.html"
      provides: "Generic PAT-RL-agnostic HGF viewer template with injection markers + Jinja2 CDN block + P10-guarded initial state"
      min_lines: 400
      contains: "viz-network-spec"
    - path: "figures/patrl_hgf_model.html"
      provides: "PAT-RL seed template preserved unchanged as phase-22 artifact (do NOT edit)"
      min_lines: 676
      contains: "PHENOS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability row for TEMPLATE-09 updated to `Partial — Phase 24`"
      min_lines: 120
      contains: "TEMPLATE-09"
  key_links:
    - from: "figures/hgf_viewer.html <script type=\"application/json\" id=\"viz-network-spec\">"
      to: "React module-scope NETWORK_SPEC constant"
      via: "document.getElementById('viz-network-spec').textContent + JSON.parse"
      pattern: "getElementById\\(['\"]viz-network-spec['\"]\\)"
    - from: "figures/hgf_viewer.html React component initial state"
      to: "NETWORK_SPEC.nodes + NETWORK_SPEC.input_idxs (NOT a hardcoded string)"
      via: "INITIAL_HGF_LEVEL derivation at module scope, consumed by useState"
      pattern: "useState\\(INITIAL_HGF_LEVEL\\)"
    - from: "figures/hgf_viewer.html CDN fallback block"
      to: "cdnjs.cloudflare.com React/React-DOM 18.2.0 + Babel 7.23.2"
      via: "{% if react_inlined %}{% else %}<script src=...>{% endif %} Jinja2 block"
      pattern: "react/18\\.2\\.0|babel-standalone/7\\.23\\.2"
---

<objective>
Promote `figures/patrl_hgf_model.html` (676 lines, PAT-RL-specific) to
the generic `figures/hgf_viewer.html` by applying every removal and
injection slot specified in TEMPLATE_AUDIT.md (plan 23-01 artifact).
The original seed is preserved unchanged. Also update REQUIREMENTS.md
TEMPLATE-09 traceability to `Partial — Phase 24` (v1.3 ships a generic
placeholder; spec-derived rendering deferred).

Purpose: TEMPLATE-01 through TEMPLATE-10, and the P10 guard. The
generic template must be browser-openable WITHOUT running Python (the
"Strategy B" property from research §2 — embed a valid default JSON
blob in the template, string-surgery replace at render time). This
is load-bearing for manual QA and for sending the bare template to
collaborators.

Output: `figures/hgf_viewer.html` (new file; ~400-500 lines after
PHENOS/DHR/EV/Google-Fonts excisions) + TEMPLATE-09 traceability fix
in REQUIREMENTS.md.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-export-template/23-RESEARCH.md
@.planning/phases/23-export-template/TEMPLATE_AUDIT.md
@.planning/phases/23-export-template/23-01-SUMMARY.md
@figures/patrl_hgf_model.html
@data/viz_fixtures/patrl_3level_prefit.json
@src/prl_hgf/viz/schema.py
@.planning/REQUIREMENTS.md

# CRITICAL DISCREPANCIES (ROADMAP vs RESEARCH):
# - Research §6 / finding #5: P10 guard does NOT require a NetworkSpec
#   schema change. Do NOT add an `hgf_level` field to NetworkSpec. Derive
#   it JS-side from `NETWORK_SPEC.nodes.some(n => n.level === 3)` +
#   `NETWORK_SPEC.input_idxs.length > 1`. This preserves Phase 22's
#   frozen schema contract.
# - Research finding #3 / §2: Do NOT use Jinja2 `{{ }}` tokens inside
#   <script type="application/json"> tags. If you did, opening the raw
#   template in a browser would trigger JSON.parse SyntaxError. Use
#   Strategy B: bake a valid default JSON blob into the template; plan
#   23-03's export.py does string-surgery replacement at render time.
# - Research §3 / finding #4: CDN URLs MUST be version-pinned to
#   react/18.2.0, react-dom/18.2.0, babel-standalone/7.23.2. The seed
#   already uses these; keep them. Do NOT bump to `18` or `latest`.
# - Research §7: The seed template's 2-level graceful rendering
#   (is3 ? ... : ... branches on rowI, SH, level bands) ALREADY works
#   after excisions. You do NOT need to add new layout code. You DO need
#   to verify by opening the rendered template with a 2-level default
#   JSON in the browser — plan 23-04 test coverage will lock this.
# - Research §10.5 / Appendix A.1: the current fixture JSON at
#   data/viz_fixtures/patrl_3level_prefit.json has `visual` and
#   `parameters` keys NOT in NetworkSpec. For the baked-in default JSON
#   blob, use the NetworkSpec-compatible SUBSET only (n_nodes, input_idxs,
#   nodes with idx/kind/level/role/branch_idx/value_parents/
#   volatility_parents/value_children/volatility_children, coord_name,
#   source_pyhgf_version, is_laplace). DROP visual/parameters/task/model
#   keys. This keeps the template's default render aligned with what
#   render_viewer_html(spec) will produce in plan 23-03.
# - Research §10.9: React 18.2.0 uses ReactDOM.createRoot API (line 673
#   of seed) — preserve verbatim. Do not "modernize" to React 19.
# - EXPORT-05 clause (b): seed imports Source Sans 3 + STIX Two Text from
#   fonts.googleapis.com at lines 7 and 9 (verified by 23-01 audit grep).
#   Both <link> tags MUST be excised in task 1. v1.3 uses system-font
#   fallback; Google Fonts inlining is deferred to v1.4 POLISH-05.
# - TEMPLATE-09 scope reality check: NetworkSpec has NO response_model
#   field as of Phase 22. Hand-rolling a spec.response_model lookup path
#   in v1.3 is out of scope. Plan replaces the PAT-RL response-model
#   equation panel with a generic hardcoded placeholder and updates
#   REQUIREMENTS.md traceability to `Partial — Phase 24`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy seed to hgf_viewer.html and apply all (R) excisions from TEMPLATE_AUDIT.md (incl. Google Fonts)</name>
  <files>figures/hgf_viewer.html</files>
  <action>
STEP 1 — Create `figures/hgf_viewer.html` as a byte-exact copy of
`figures/patrl_hgf_model.html`:
```bash
cp figures/patrl_hgf_model.html figures/hgf_viewer.html
```
Confirm the seed is unchanged: `diff figures/patrl_hgf_model.html <(git show HEAD:figures/patrl_hgf_model.html)` must be empty.

STEP 2 — Apply every (R) classification from TEMPLATE_AUDIT.md. Working
from research §1 categories:

**Google Fonts block (R — EXPORT-05 clause (b)):**
- Line 7: delete `<link rel="preconnect" href="https://fonts.googleapis.com">`.
- Line 8 (if present): delete any paired `<link rel="preconnect"
  href="https://fonts.gstatic.com" crossorigin>`.
- Line 9: delete the `<link href="https://fonts.googleapis.com/css2?family=
  Source+Sans+3...STIX+Two+Text..." rel="stylesheet">` stylesheet.
- Do NOT remove the inline `fontFamily="STIX Two Text,Georgia,serif"`
  attributes throughout the SVG — those provide graceful fallback to
  system Georgia/serif when Google Fonts are absent. The existing CSS
  chain already works system-font-only.
- Rationale: v1.3 system-font fallback per EXPORT-05 clause (b);
  POLISH-05 defers base64 `@font-face` inlining to v1.4.

**Phenotype block (R):**
- Lines ~45-51: delete the `const PC = {balanced, reward_blunted,
  threat_hypervigilant, both_disrupted}` phenotype color map.
- Lines ~53-87: delete the entire `const PHENOS = [...]` array (4
  phenotype definitions).
- Lines ~125: delete `const [phenoId, setPhenoId] = useState("balanced")`.
- Line ~128: delete `const pheno = PHENOS.find(...)`.
- Lines ~134-136: delete `hlN`, `hlE`, `hlP` highlight helpers.
- Lines ~156-158: rewrite `nC`, `eC`, `pC` as identity pass-throughs
  (they currently call hlN/hlE/hlP). Final form:
  ```jsx
  const nC = (baseColor) => baseColor;
  const eC = (baseColor) => baseColor;
  const pC = (baseColor) => baseColor;
  ```
  OR delete entirely and inline the base colors at call sites — pick
  whichever minimizes diff surface.
- Lines ~231-244: delete the entire `<Blk title="ANS PHENOTYPES (2×2)">`
  sidebar block.

**Response-model radio buttons (R — TEMPLATE-09 partial):**
- Line ~122: delete `const [respModel, setRespModel] = useState("C");`.
- Line ~131: delete `const hasDHR = respModel === "B" || respModel === "C";`.
- Line ~132: delete `const hasAlpha = respModel === "C";`.
- Lines ~216-229: delete the entire `<Blk title="RESPONSE MODEL">`
  sidebar block (Model A/B/C radio buttons).
- Lines ~343-357: simplify the response-model equation panel to a SINGLE
  static equation placeholder. For v1.3, use a generic hardcoded text
  like `"Decision equation: rendered per response model (injected)"`
  with a comment `{/* TEMPLATE-09 PARTIAL: generic placeholder only;
  spec-derived rendering from NetworkSpec.response_model deferred to
  Phase 24 pending schema extension */}`.
  Preserve the horizontal separator above this block (TEMPLATE-09
  "below a horizontal separator line" clause — the separator itself is
  kept; only the PAT-RL bilinear text body is stripped).
  Per research §1 and §10.5 recommendation (a), NetworkSpec does NOT
  currently carry a response-model block; this is a Phase 24+ concern.
  Do NOT hand-craft a spec.response_model lookup path today.

**DHR node + edges (R):**
- Line ~40: delete the `dhr: {...}` entry from the `C` color palette.
- Line ~42: delete `eDHR: "#388E3C"` palette entry.
- Lines ~576-607: delete the entire `{hasDHR && (...)}` ΔHR SVG block
  (input node + γ/α edges).
- Lines ~540-550: delete the `Vᵣ` / `Vₛ` reward/shock magnitude diamond
  nodes (PAT-RL 2x2 magnitudes).
- Line ~251: delete the `{sh:"dia", c:C.dhr, t:"ΔHR / magnitude input"}`
  entry from the NOTATION sidebar shape legend.

**EV band + computation block (R):**
- Line ~302: delete the `<rect ...fill={C.ev.stroke}...>` EV band
  background rectangle.
- Lines ~534-562: delete the entire `EV = (1-P_d)·Vᵣ - P_d·Vₛ`
  computation block and its surrounding SVG elements.

**Citations / bradycardia references (R):**
- Lines ~110-111: when deleting the `dhr_ev` and `dhr_resp` INFO dict
  entries (next step), the `bradycardia` references go with them.

**INFO dict bodies (R for PAT-RL-specific bodies, keep title strings):**
- Lines ~91-112: in the `const INFO = {...}` object, KEEP the keys
  (node identifiers like `"u"`, `"belief"`, `"vol"`, `"response"`,
  etc.) but STRIP PAT-RL-specific body text — replace each body with a
  short generic tooltip or empty string. Example transformation:
  - Before: `u: "u — Binary state observation (node 0 in pyhgf). Hazard rate: h=0.03/0.10 driving reversals."`
  - After: `u: "u — Binary state observation (input node)."`
  - Before: `response: "... V_r / V_s reward magnitude {1,3} / shock magnitude {1,3} ... response_patrl.py ... Model A/B/C equations ..."`
  - After: `response: "Response model node."`

  DELETE entirely: `dhr`, `dhr_ev`, `dhr_resp`, `gamma`, `alpha`,
  `V_r`, `V_s` keys (DHR + PAT-RL magnitudes, classified R).

  PRESERVE: `u`, `belief`, `vol`, `mu2`, `mu3`, `response`, `kappa`,
  `omega2`, `omega3`, `beta`, `zeta`, `b` (generic HGF concepts the
  generic viewer still renders).

**Title (I — change to generic):**
- Line 6: `<title>PAT-RL HGF Model</title>` → `<title>HGF Viewer</title>`
  (hardcoded string; no injection token. For v1.3 scope the title is
  static generic; Phase 24 can introduce a spec.title injection if
  needed).

STEP 3 — Post-excision sanity check:
```bash
# All (R) audit terms must be gone:
grep -cE 'balanced|reward_blunted|threat_hypervigilant|both_disrupted' figures/hgf_viewer.html
# expect: 0
grep -cE 'PHENOS|C\.dhr|response_patrl|shock_mag|reward_mag|bradycardia' figures/hgf_viewer.html
# expect: 0
# Google Fonts excised (EXPORT-05 clause (b)):
grep -cE 'googleapis|@import.*font|fonts\.googleapis\.com' figures/hgf_viewer.html
# expect: 0
```

If any of these return > 0, re-inspect and apply the missed excision
before commit.

STEP 4 — Preserve unchanged:
- `hexPts`, `diamPts`, `retract` helpers (lines ~115-117) — KEEP verbatim.
- `is3` / `isMM` branching throughout (research §7) — KEEP; 2-level
  graceful rendering is emergent from this.
- Notation sidebar block (lines ~247-275) — KEEP except the two removed
  entries above.
- `ReactDOM.createRoot(...)` final render (line ~673) — KEEP verbatim.
- `fontFamily="STIX Two Text,Georgia,serif"` inline attributes throughout
  the SVG — KEEP (TEMPLATE-06; system-font fallback chain).

COMMIT MESSAGE: `feat(23-02): promote patrl_hgf_model.html to generic hgf_viewer.html via (R) excisions from TEMPLATE_AUDIT.md (incl. Google Fonts)`.
  </action>
  <verify>
- `test -f figures/hgf_viewer.html` succeeds.
- `wc -l figures/patrl_hgf_model.html figures/hgf_viewer.html` — seed still 676; new file between 400 and 600 lines.
- `diff figures/patrl_hgf_model.html <(git show HEAD~:figures/patrl_hgf_model.html) 2>/dev/null | wc -l` — 0 (seed untouched).
- `grep -cE 'balanced|reward_blunted|threat_hypervigilant|both_disrupted|PHENOS|C\.dhr|response_patrl|shock_mag|reward_mag|bradycardia' figures/hgf_viewer.html` — 0.
- `grep -cE 'googleapis|@import.*font|fonts\.googleapis\.com' figures/hgf_viewer.html` — 0 (EXPORT-05 clause (b) standing guard).
- `grep -c 'hexPts\|diamPts\|retract' figures/hgf_viewer.html` — >=3 (helper geometry preserved).
- `grep -c 'ReactDOM.createRoot' figures/hgf_viewer.html` — 1 (mount call preserved).
- `grep -c 'STIX Two Text' figures/hgf_viewer.html` — >=4 (inline font-family attributes preserved for TEMPLATE-06 system-font fallback).
  </verify>
  <done>
hgf_viewer.html is a promoted copy of the seed with every (R)-classified
region from TEMPLATE_AUDIT.md excised (including Google Fonts `<link>`
tags). All 11 audit terms' R-classified hits now return 0 in the new
file, and Google Fonts grep returns 0. Seed template preserved
bit-identical. The file still parses as HTML (bs4 will confirm in plan
23-04); no in-browser test yet (that comes after task 2 adds injection
markers).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply (I) injection slots — P10 guard, marker tags with default JSON, Jinja2 CDN block + REQUIREMENTS.md TEMPLATE-09 traceability update</name>
  <files>figures/hgf_viewer.html, .planning/REQUIREMENTS.md</files>
  <action>
Continue editing `figures/hgf_viewer.html` (from task 1). Apply (I)
injection slots. Also update `.planning/REQUIREMENTS.md` traceability row
for TEMPLATE-09 to reflect the v1.3 `Partial — Phase 24` reality.

**Step A — P10 guard (line ~121 replacement):**

The seed has:
```jsx
const [hgfLevel, setHgfLevel] = React.useState("3level");
```

Replace with the canonical pattern from research §6 / §7 + derivation
from the module-scope `NETWORK_SPEC` constant that task 2 step C adds:
```jsx
const INITIAL_HGF_LEVEL = (() => {
  const maxL = NETWORK_SPEC.nodes.reduce((m, n) => Math.max(m, n.level), 0);
  if (maxL >= 3) return "3level";
  if (NETWORK_SPEC.input_idxs.length > 1) return "2level_mm";
  return "2level";
})();
```
Place this ABOVE the `function App() {...}` declaration (module scope
inside the `<script type="text/babel">` block). Inside `App()`, change
the useState call to:
```jsx
const [hgfLevel, setHgfLevel] = React.useState(INITIAL_HGF_LEVEL);
```

**Step B — Inject the two marker tags with VALID DEFAULT JSON bodies:**

Inside `<head>` or at the top of `<body>`, BEFORE the `<script
type="text/babel">` block (so the tags exist when the Babel script runs
document.getElementById), add:

```html
<!-- v1.3 injection markers (EXPORT-02). render_viewer_html does string-surgery
     on the content between these tag delimiters. The default content below is a
     valid NetworkSpec so the template renders a meaningful visualization when
     opened directly in a browser without Python (research §2 Strategy B). -->
<script type="application/json" id="viz-network-spec">
{
  "n_nodes": 3,
  "input_idxs": [0],
  "nodes": [
    {"idx": 0, "kind": "binary-state", "level": 1, "role": "input", "branch_idx": 0,
     "value_parents": [1], "volatility_parents": [], "value_children": [], "volatility_children": []},
    {"idx": 1, "kind": "continuous-state", "level": 2, "role": "value", "branch_idx": 0,
     "value_parents": [], "volatility_parents": [2], "value_children": [0], "volatility_children": []},
    {"idx": 2, "kind": "continuous-state", "level": 3, "role": "volatility", "branch_idx": 0,
     "value_parents": [], "volatility_parents": [], "value_children": [], "volatility_children": [1]}
  ],
  "coord_name": null,
  "source_pyhgf_version": "0.2.8",
  "is_laplace": false
}
</script>
<script type="application/json" id="viz-posterior-summary"></script>
```

Note: the `viz-posterior-summary` tag body is intentionally EMPTY in
v1.3 pre-fit mode. Phase 24 (POST-02) populates it when `idata` is
provided to `build_network_spec`. Do NOT put `null` or `{}` in the
body — empty is the correct pre-fit signal.

**IMPORTANT:** The regex in plan 23-03 `_inject_markers` uses
`re.DOTALL` and matches content BETWEEN `<script type="application/json"
id="viz-XXX">` and `</script>`. The default body JSON MUST NOT contain
a literal `</script>` substring (it doesn't — but a future edit might
inadvertently add one). Do NOT add HTML-y string values into the default
body.

**Step C — Module-scope NETWORK_SPEC + POSTERIOR_SUMMARY constants:**

At the very top of the `<script type="text/babel">` block, BEFORE any
component declarations and BEFORE the P10 `INITIAL_HGF_LEVEL` code from
step A, add (research §6 canonical pattern):

```jsx
const NETWORK_SPEC = JSON.parse(
  document.getElementById("viz-network-spec").textContent
);

const POSTERIOR_SUMMARY = (() => {
  const el = document.getElementById("viz-posterior-summary");
  if (!el || !el.textContent.trim()) return null;
  try {
    return JSON.parse(el.textContent);
  } catch {
    return null;
  }
})();
```

Rationale: parse once at module load; defensive null-handling for
empty posterior tag in pre-fit mode.

**Step D — Jinja2 CDN block ({% if react_inlined %}):**

Find the three `<script src="https://cdnjs.cloudflare.com/...">` tags
in the current template (React, React-DOM, Babel — seed lines near
the top of `<head>`). Wrap them in a Jinja2 conditional:

```html
{% if react_inlined and react_dom_inlined and babel_inlined %}
<script>{{ react_inlined | safe }}</script>
<script>{{ react_dom_inlined | safe }}</script>
<script>{{ babel_inlined | safe }}</script>
{% else %}
<script src="https://cdnjs.cloudflare.com/ajax/libs/react/18.2.0/umd/react.production.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.2.0/umd/react-dom.production.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/babel-standalone/7.23.2/babel.min.js"></script>
{% endif %}
```

Version pins are literal (research §10.9 warns against `18` or
`latest`). The `| safe` filter on inlined assets prevents Jinja2's
autoescape from mangling the JS source (research §10.10).

Note: Because this template ships with the `{% if %}` block, opening
the raw template in a browser WITHOUT rendering causes the browser to
see the literal `{% if %}` tokens as HTML text. This is benign — HTML
parsers ignore unknown `{` sequences in content — and both `{% if %}`
and `{% else %}` branch `<script>` tags will load. Browser will then
execute BOTH inlined branch (empty, since `react_inlined` is not
substituted) AND CDN fallback branch (working). Net effect: raw-open
fetches from CDN, which is the desired "it renders when opened bare"
behavior.

**Step E — REQUIREMENTS.md TEMPLATE-09 traceability update:**

Edit `.planning/REQUIREMENTS.md` traceability table (~line 132):
```
| TEMPLATE-09 | Phase 23 | Pending |
```
Change to:
```
| TEMPLATE-09 | Phase 23 / 24 | Partial — generic placeholder in Phase 23; spec-derived rendering requires NetworkSpec.response_model field (Phase 24) |
```

Do NOT change any other traceability row. Keep the coverage counts line
(`v1.3 requirements: 38 total`) unchanged — TEMPLATE-09 is still a v1.3
requirement, just split across two phases.

VERIFY this claim by opening the raw template in a browser after step D
— the PAT-RL 3-level topology should render from the default JSON blob
(step B) via the CDN-fetched React (step D else-branch).

COMMIT MESSAGE: `feat(23-02): add NETWORK_SPEC marker tags + P10 guard + Jinja2 CDN block + TEMPLATE-09 traceability -> Partial (TEMPLATE-01, -04, -07, -09, EXPORT-02, EXPORT-05)`.
  </action>
  <verify>
- `grep -c 'viz-network-spec' figures/hgf_viewer.html` — >=2 (the <script> tag + the JS getElementById call).
- `grep -c 'viz-posterior-summary' figures/hgf_viewer.html` — >=2 (same pattern for the second marker).
- `grep -c 'useState("3level")' figures/hgf_viewer.html` — 0 (P10 guard: hardcoded default excised).
- `grep -c 'INITIAL_HGF_LEVEL\|NETWORK_SPEC' figures/hgf_viewer.html` — >=2 (derivation in place).
- `grep -c 'react/18.2.0/umd' figures/hgf_viewer.html` — 1 (version-pinned CDN fallback preserved).
- `grep -c 'babel-standalone/7.23.2' figures/hgf_viewer.html` — 1.
- `grep -c '{% if react_inlined' figures/hgf_viewer.html` — 1.
- `python -c "import json; d=open('figures/hgf_viewer.html').read(); import re; m=re.search(r'<script type=\"application/json\" id=\"viz-network-spec\">(.*?)</script>', d, re.DOTALL); json.loads(m.group(1))"` — succeeds (default JSON blob is valid JSON).
- `grep -n 'TEMPLATE-09 | Phase 23 / 24 | Partial' .planning/REQUIREMENTS.md` — 1 match (traceability row updated).
- `grep -c 'TEMPLATE-09 | Phase 23 | Pending' .planning/REQUIREMENTS.md` — 0 (old row removed).
  </verify>
  <done>
hgf_viewer.html now has: two injection marker `<script type="application/json">`
tags (viz-network-spec with valid default body, viz-posterior-summary empty
for pre-fit); module-scope NETWORK_SPEC + POSTERIOR_SUMMARY constants
parsed from those tags; INITIAL_HGF_LEVEL derived from spec (P10 guard);
useState call wired to INITIAL_HGF_LEVEL; Jinja2 `{% if react_inlined %}`
block wraps version-pinned CDN fallback tags. Default JSON blob is valid
and matches the PAT-RL 3-level NetworkSpec-compatible subset.
REQUIREMENTS.md TEMPLATE-09 row updated to `Partial — Phase 24` to
reflect the v1.3 generic-placeholder reality.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-open verification — bare template renders PAT-RL 3-level topology + multimodal 2-level toggle</name>
  <what-built>
`figures/hgf_viewer.html` is a PAT-RL-agnostic generic HGF viewer
template with:
- All R-classified PHENOS / DHR / EV / response-radio / Google-Fonts
  surface excised.
- Two `<script type="application/json">` injection markers (default
  body = PAT-RL 3-level NetworkSpec subset).
- Module-scope NETWORK_SPEC / POSTERIOR_SUMMARY parsing.
- P10-guarded initial state derivation (INITIAL_HGF_LEVEL via
  nodes.reduce + input_idxs.length).
- Version-pinned CDN fallback (React 18.2.0, React-DOM 18.2.0, Babel
  7.23.2) inside Jinja2 `{% if %}` block.
- System-font fallback only (Georgia/serif + STIX Two Text inline
  font-family attributes; no Google Fonts <link> tags).

The key property per research §2 Strategy B: opening this raw template
in a browser WITHOUT running render_viewer_html should render the
PAT-RL 3-level HGF topology from the baked-in default JSON blob. No
console errors, no JSON.parse failures.

Automated checks can verify the file structure but NOT the browser
render. This checkpoint is where the "it opens in a browser" property
is human-confirmed, including TEMPLATE-08 multimodal mode (two input
diamonds -> one shared belief circle).
  </what-built>
  <how-to-verify>
1. Open `figures/hgf_viewer.html` directly in a modern browser
   (Chrome/Firefox/Edge). Use file:// protocol (double-click the file,
   or paste the absolute path into the URL bar).

2. Wait 2-5 seconds for the CDN to load React + Babel. Spinner/blank
   is normal during this window.

3. **TEMPLATE-03 + TEMPLATE-04 shape + band check (3-level):**
   - Three horizontal level bands visible (L1 blue bottom, L2 teal
     middle, L3 coral top — TEMPLATE-04 canonical palette:
     L3 `#FAECE7` fill / `#993C1D` stroke, L2 `#E1F5EE`/`#0F6E56`,
     L1 `#E6F1FB`/`#185FA5`).
   - A diamond node at L1 (TEMPLATE-03 binary input, idx=0).
   - A circle node at L2 (TEMPLATE-03 value parent, idx=1).
   - A hexagon node at L3 (TEMPLATE-03 volatility parent, idx=2).
   - Coupling edges between them (TEMPLATE-05: solid blue L1->L2,
     dashed coral L2->L3 with κ label on the volatility edge).

4. **TEMPLATE-06 parameter glyph check:** confirm the sidebar / notation
   block shows the STIX-rendered glyphs ω₂, ω₃, κ, β, ζ, b, μ₃⁰ (fall
   back to Georgia serif if Google Fonts absent — they still render,
   just with slightly different weighting).

5. Open browser DevTools console (F12). Confirm ZERO errors. Warnings
   about "development mode" / "deprecated lifecycle" from React are
   OK; JSON.parse errors or "undefined" TypeErrors are NOT.

6. **TEMPLATE-10 notation legend:** confirm the NOTATION sidebar
   (bottom-right or equivalent) is visible WITHOUT clicking anything,
   and lists shape/edge legend entries — WITHOUT the ΔHR / magnitude
   entry removed in task 1.

7. **TEMPLATE-09 partial check:** confirm the response-model panel below
   the horizontal separator shows the generic placeholder text
   ("Decision equation: rendered per response model (injected)") — NOT
   the PAT-RL Model A/B/C bilinear equations. This is expected v1.3
   `Partial` behavior; full spec-derived rendering lands in Phase 24.

8. Confirm the "HGF LEVEL" sidebar radio buttons (2-level / 3-level
   / multimodal) are visible; 3-level should be pre-selected (because
   INITIAL_HGF_LEVEL resolved to "3level" from the default JSON).

9. **TEMPLATE-07 — 2-level toggle:** click the "2-level" radio. Confirm:
   L3 band disappears entirely (NO dead space per TEMPLATE-07; research
   §7 verified this is emergent from is3 ternaries). Volatility hexagon
   and its κ edge disappear.

10. **TEMPLATE-08 — multimodal 2-level toggle:** click the "multimodal"
    radio. Confirm: TWO input diamonds (u₁ and u₂) are rendered at the
    L1 band, BOTH coupling via solid blue value edges to ONE shared
    belief circle at the L2 band. No L3 band, no volatility hexagon.
    This is the key TEMPLATE-08 property — two inputs, one shared
    parent.

11. Click back to "3-level". Confirm full topology returns.

If ANY of steps 3-10 fail, the template promotion is incomplete. Record
what failed in the resume signal.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 11 steps pass.
If any step fails, describe what you saw and which step number — plan
23-02 must re-open with a corrective task before dependents (23-03,
23-04) run.
  </resume-signal>
</task>

</tasks>

<verification>
**Plan 23-02 close checks (run before commit):**

1. Audit terms excised:
   ```bash
   grep -cE 'balanced|reward_blunted|threat_hypervigilant|both_disrupted|PHENOS|C\.dhr|response_patrl|shock_mag|reward_mag|bradycardia' figures/hgf_viewer.html
   # expect: 0
   ```

2. EXPORT-05 clause (b) standing guard (Google Fonts excised):
   ```bash
   grep -cE 'googleapis|@import.*font|fonts\.googleapis\.com' figures/hgf_viewer.html
   # expect: 0
   ```

3. Injection markers present:
   ```bash
   grep -E '<script type="application/json" id="viz-(network-spec|posterior-summary)">' figures/hgf_viewer.html | wc -l
   # expect: 2
   ```

4. P10 guard grep (research §6 final verification):
   ```bash
   grep -c 'useState("3level")' figures/hgf_viewer.html    # expect: 0
   grep -c 'INITIAL_HGF_LEVEL' figures/hgf_viewer.html     # expect: >=2
   grep -c 'NETWORK_SPEC\.nodes' figures/hgf_viewer.html   # expect: >=1
   ```

5. Version-pinned CDN URLs:
   ```bash
   grep -c 'react/18.2.0/umd\|react-dom/18.2.0/umd\|babel-standalone/7.23.2' figures/hgf_viewer.html
   # expect: 3
   ```

6. Default JSON blob parses:
   ```bash
   conda run -n ds_env python -c "
   import json, re
   s = open('figures/hgf_viewer.html').read()
   m = re.search(r'<script type=\"application/json\" id=\"viz-network-spec\">(.*?)</script>', s, re.DOTALL)
   d = json.loads(m.group(1))
   assert d['n_nodes'] == 3
   assert d['input_idxs'] == [0]
   print('default blob parses; n_nodes =', d['n_nodes'])
   "
   ```

7. REQUIREMENTS.md TEMPLATE-09 traceability updated:
   ```bash
   grep -c 'TEMPLATE-09 | Phase 23 / 24 | Partial' .planning/REQUIREMENTS.md  # expect: 1
   grep -c 'TEMPLATE-09 | Phase 23 | Pending' .planning/REQUIREMENTS.md       # expect: 0
   ```

8. Seed template unchanged:
   ```bash
   git diff HEAD~<plan-23-01-commit-count>..HEAD -- figures/patrl_hgf_model.html | wc -l
   # expect: 0
   ```

9. Parallel-stack invariant:
   ```bash
   git diff --stat src/prl_hgf/env/ src/prl_hgf/models/ src/prl_hgf/fitting/ src/prl_hgf/analysis/ src/prl_hgf/gui/ src/prl_hgf/power/ src/prl_hgf/simulation/
   # expect: empty
   ```

10. Browser-open human verification (task 3 checkpoint) — must be
    "approved" for all 11 sub-steps including TEMPLATE-08 multimodal.
</verification>

<success_criteria>
- figures/hgf_viewer.html exists as a new file; figures/patrl_hgf_model.html
  preserved byte-identical.
- All (R) regions from TEMPLATE_AUDIT.md excised (PHENOS, DHR, EV,
  response-radio, Google Fonts).
- Two injection markers (viz-network-spec with valid default body,
  viz-posterior-summary empty).
- P10 guard satisfied — no hardcoded useState("3level"), derivation
  from NETWORK_SPEC in place.
- Jinja2 {% if react_inlined %} block with version-pinned CDN fallback.
- Browser-open renders PAT-RL 3-level topology with 2-level AND
  multimodal 2-level toggle working (research §7 emergent from is3/isMM
  branching; TEMPLATE-07 + TEMPLATE-08 interactively verified).
- TEMPLATE-09 `Partial` reality reflected in REQUIREMENTS.md traceability.
- Parallel-stack invariant holds.
</success_criteria>

<output>
After completion, create
`.planning/phases/23-export-template/23-02-SUMMARY.md` using the
execute-plan summary template. Include:
- Line count delta (seed 676 → new ~400-500).
- Diff-stat summary per excision category (PHENOS: N lines removed, DHR:
  M lines removed, Google Fonts: 2 `<link>` tags removed, etc.).
- The full default JSON blob embedded (as the canonical reference for
  plan 23-03's test fixtures to align against).
- Human-verification result from the task-3 checkpoint (which browser,
  which of the 11 steps passed/failed, including TEMPLATE-08 multimodal).
- REQUIREMENTS.md diff (TEMPLATE-09 row Pending → Partial — Phase 24).
- Confirmation `src/prl_hgf/viz/export.py` still does not exist at plan
  close (plan 23-03's scope).
</output>
</output>

---
phase: 06-group-analysis
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - manuscript/_quarto.yml
  - manuscript/paper.qmd
  - manuscript/references.bib
  - manuscript/.gitignore
autonomous: true

must_haves:
  truths:
    - "Quarto manuscript project renders to PDF via quarto render"
    - "Inline Python code cells load results CSVs and display computed statistics"
    - "BMS results are included in the main Results section"
    - "omega_3 is reported with an explicit recovery caveat"
  artifacts:
    - path: "manuscript/_quarto.yml"
      provides: "Quarto project config with arxiv-pdf format"
      contains: "arxiv-pdf"
    - path: "manuscript/paper.qmd"
      provides: "Main manuscript with sections, inline stats, and figure code cells"
      min_lines: 100
    - path: "manuscript/references.bib"
      provides: "BibTeX entries for key references"
      contains: "Mathys"
    - path: "manuscript/.gitignore"
      provides: "Ignores rendered output (PDF, tex, _freeze)"
      contains: "_freeze"
  key_links:
    - from: "manuscript/paper.qmd"
      to: "results/"
      via: "pd.read_csv('../results/...')"
      pattern: "read_csv.*results"
    - from: "manuscript/_quarto.yml"
      to: "quarto-arxiv extension"
      via: "format: arxiv-pdf"
      pattern: "arxiv-pdf"
---

<objective>
Scaffold the Quarto scientific manuscript at manuscript/ with arXiv preprint format,
inline Python statistics from results CSVs, and placeholder sections for Introduction,
Methods, Results, and Discussion. The manuscript is a self-contained Quarto project
that re-renders with updated statistics whenever the pipeline reruns.

Purpose: Provide the publication scaffold that consumes pipeline outputs. Figures will
be rendered inside Quarto code cells (not pre-generated PNGs). BMS in main Results.
All statistics inline via `{python}` expressions.

Output: manuscript/ folder with _quarto.yml, paper.qmd, references.bib, .gitignore.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-group-analysis/06-CONTEXT.md
@.planning/phases/06-group-analysis/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quarto manuscript scaffold</name>
  <files>
    manuscript/_quarto.yml
    manuscript/paper.qmd
    manuscript/references.bib
    manuscript/.gitignore
  </files>
  <action>
  1. Create the `manuscript/` directory at the project root.

  2. **manuscript/_quarto.yml**:
  ```yaml
  project:
    type: manuscript

  manuscript:
    article: paper.qmd

  format:
    html: default
    arxiv-pdf:
      keep-tex: true

  jupyter: python3

  execute:
    echo: false
    # Set cache: false during active development.
    # Quarto cache does NOT detect changes in external CSV files.
    # After results stabilize, switch to cache: true and use
    # quarto render --cache-refresh paper.qmd when results change.
    cache: false
  ```

  3. **manuscript/paper.qmd** -- a structured Quarto manuscript with these sections:

  **YAML frontmatter:**
  - title: "Psilocybin modulates belief updating in post-concussion syndrome: An HGF analysis of probabilistic reversal learning"
  - author: Adam Manoogian (Monash University, Turner Institute for Brain and Mental Health)
  - date: today
  - abstract: A 3-4 sentence placeholder abstract mentioning HGF, PRL, group x session design, and simulated validation.
  - bibliography: references.bib

  **Setup code cell (hidden, label: setup):**
  ```python
  import pandas as pd
  import numpy as np
  from pathlib import Path
  results_dir = Path("../results/group_analysis")
  # Load results CSVs -- these are produced by scripts/06_group_analysis.py
  # Wrap in try/except so the manuscript renders even without results
  try:
      estimates = pd.read_csv(results_dir / "estimates_wide.csv")
      contrasts = pd.read_csv(results_dir / "group_contrasts.csv")
      effect_sizes = pd.read_csv(results_dir / "effect_sizes.csv")
      has_results = True
  except FileNotFoundError:
      has_results = False
  ```

  **Section: Introduction**
  - Placeholder paragraph on HGF and computational psychiatry for belief updating.
  - Placeholder paragraph on psilocybin and reversal learning.
  - Placeholder paragraph on post-concussion syndrome.
  - Hypothesis statement: "We hypothesized that psilocybin would differentially modulate tonic volatility (omega_2) across treatment sessions in post-concussion vs control participants."
  - Use @-citations: @mathys2011, @mathys2014, @mason2024, @legrand2024.

  **Section: Methods**
  - Subsection: Participants and Design -- 2 groups x 3 sessions, placeholder for sample size.
  - Subsection: Task -- PRL pick_best_cue, 3 cues, criterion-based reversals, partial feedback.
  - Subsection: Computational Modeling
    - HGF description: 2-level and 3-level binary HGF with 3 parallel branches.
    - Response model: softmax + stickiness.
    - Parameter table: omega_2, omega_3, kappa, beta, zeta with descriptions.
  - Subsection: Statistical Analysis
    - Two-stage approach: "Following standard practice in computational psychiatry [e.g., @iglesias2021], posterior means from individual MCMC fits were used as point estimates in the group-level model."
    - Bambi formula: group x session interaction with random intercepts.
    - Effect sizes: Cohen's d and 94% HDI from Bayesian posterior.
    - Model comparison: random-effects BMS (Rigoux et al. 2014).

  **Section: Results**
  - Subsection: Parameter Recovery (brief, references Phase 5 validation).
  - Subsection: Model Comparison (BMS) -- inline stats from results CSVs:
    - Code cell to load BMS summary CSV and display exceedance probabilities.
    - Inline stat: "The [winning model] showed an exceedance probability of `{python} f'{xp:.2f}'`."
  - Subsection: Group x Session Effects on omega_2 (primary hypothesis)
    - Code cell to extract omega_2 contrast row from contrasts DataFrame.
    - Inline: "The group x session interaction on omega_2 yielded a posterior mean difference of `{python} f'{omega2_mean:.3f}'` (94% HDI: [`{python} f'{omega2_lo:.3f}'`, `{python} f'{omega2_hi:.3f}'`])."
    - Code cell to render raincloud plot for omega_2 (using ptitprince, loading estimates CSV).
  - Subsection: Secondary Parameters
    - Brief results for kappa, beta, zeta with inline stats.
    - omega_3 with explicit caveat: "Note: omega_3 parameter recovery was poor in our validation analysis (Phase 5); results for this parameter should be interpreted with caution."
  - Subsection: Effect Sizes
    - Code cell to display effect_sizes DataFrame as a formatted table.

  **Section: Discussion**
  - Placeholder paragraphs for: main findings, comparison to Mason et al. 2024, limitations (two-stage approach, simulated data, omega_3), future directions.

  **Section: References**
  - `::: {#refs}` block (Quarto handles this automatically with bibliography).

  All inline statistics should be wrapped in conditionals:
  ```
  `{python} f"{value:.3f}" if has_results else "N/A"`
  ```
  This way the manuscript renders even when results CSVs are missing.

  4. **manuscript/references.bib** -- BibTeX entries for:
  - mathys2011: "A Bayesian foundation for individual learning under uncertainty"
  - mathys2014: "Uncertainty in perception and the Hierarchical Gaussian Filter"
  - legrand2024: "pyhgf: A neural network library for predictive coding" (arXiv:2410.09206)
  - mason2024: Psilocybin + reversal learning (Translational Psychiatry)
  - rigoux2014: "Bayesian model selection for group studies - revisited"
  - iglesias2021: Reference for two-stage approach in computational psychiatry
  - weber2024: "The generalized Hierarchical Gaussian Filter"
  Use real publication details where known. For entries where exact details are uncertain, include a TODO comment.

  5. **manuscript/.gitignore**:
  ```
  # Quarto rendered output
  _freeze/
  _site/
  *.pdf
  *.tex
  *.log
  *.aux
  *.bbl
  *.blg
  *.fls
  *.fdb_latexmk
  *.synctex.gz
  _extensions/
  ```

  Use `from __future__ import annotations` in all Python code cells. All code cells should have `#| echo: false` via the global execute config.
  </action>
  <verify>
  - manuscript/_quarto.yml exists and is valid YAML.
  - manuscript/paper.qmd exists with all four major sections (Introduction, Methods, Results, Discussion).
  - manuscript/references.bib exists with at least 5 BibTeX entries.
  - manuscript/.gitignore exists and ignores _freeze/ and *.pdf.
  - The paper.qmd can be parsed by Quarto (if quarto CLI is available): `cd manuscript && quarto check` or `quarto render paper.qmd --to html` as a smoke test. If quarto is not installed, verify syntax manually.
  </verify>
  <done>
  Manuscript scaffold is complete with arXiv format, inline Python statistics from results CSVs, BMS in main Results, omega_3 caveat, and graceful fallback when results are missing. Ready for rendering once pipeline outputs exist.
  </done>
</task>

</tasks>

<verification>
- manuscript/ directory contains _quarto.yml, paper.qmd, references.bib, .gitignore.
- paper.qmd has Introduction, Methods, Results, Discussion sections.
- Results section includes inline computed statistics with conditional rendering.
- BMS results are in the main Results section (not supplementary).
- omega_3 has explicit recovery caveat in Results.
- references.bib has entries for Mathys, Mason, Rigoux, Legrand, Weber.
</verification>

<success_criteria>
1. _quarto.yml configures arxiv-pdf output with manuscript type.
2. paper.qmd loads results CSVs in a setup cell and uses inline `{python}` expressions for all statistics.
3. Raincloud plot code cell is embedded in the Results section (renders figure from data, not a static PNG).
4. omega_3 results include explicit caveat about poor recovery.
5. Manuscript renders without error even when results CSVs are missing (graceful fallback to "N/A").
</success_criteria>

<output>
After completion, create `.planning/phases/06-group-analysis/06-03-SUMMARY.md`
</output>

---
phase: 21-benchmark-bottleneck-diagnosis
plan: "07"
type: execute
wave: 3
depends_on: ["01", "02", "03", "04", "05", "06"]
files_modified:
  - .planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json
  - .planning/ROADMAP.md
  - .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md
  - .planning/STATE.md
files_created:
  - .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md
  - .planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md
autonomous: true
requirements:
  - BENCH-DIAG-05

must_haves:
  truths:
    - "VERDICT.md exists and names the single dominant bottleneck among {NUTS sampler step, HGF scan graph, JIT cache key, closure-over-data vs traced-args, shape polymorphism, tracing-cost-dominates paradox, cache-init bug, other}"
    - "VERDICT.md cites specific quantitative evidence from 21-01 scope memo, 21-03/04 vb_laplace_vs_nuts_jit.json, 21-05 jit_scaling_sweep.csv, and 21-06 cache_forensics.md for each alternative considered and each rejected"
    - "VERDICT.md includes a PR-level fix recommendation: names the file, the function, and the patch shape. The recommendation must be actionable — i.e., the reader can open a follow-up plan and implement the fix without re-investigating the problem"
    - "VERDICT.md explicitly identifies which follow-up plan number (14.1-0X gap closure OR new integer phase) will implement the fix, and what the test criterion for success is (e.g., 'after fix, cold→next_proc_warm ratio at P=50 is >= 3x; cold JIT at P=50 is < 300s')"
    - "vb_laplace_vs_nuts_jit.json bottleneck_verdict.bottleneck_layer is set to a concrete value (not 'undetermined') and bottleneck_verdict.evidence is a paragraph citing specific numbers"
    - "vb_laplace_vs_nuts_jit.json hlo_op_counts is filled from 21-01 benchmark-scope-memo.md — the per-stage HLO op counts (LBFGS body, Hessian, NUTS body scan inner) are copied in so the JSON is self-contained"
    - "VERDICT.md EXPLICITLY does NOT implement the fix. The fix is a separate follow-up plan. VERDICT.md is the evidence-based recommendation that unblocks 14.1-03 (Phase 14 BlackJAX bench re-submission) by naming the fix"
    - "ROADMAP.md Phase 21 SC1/SC2/SC4 text is brought into sync with CONTEXT.md before phase close (state-space-only baselines for SC1; vb_laplace_vs_nuts_jit.json filename + 7-value enum + VB-Laplace probe wording for SC2; P ∈ {5, 20, 50} with rationale for SC4; status column replaces jit_wall_clock_s for SC4)"
    - "CONTEXT.md §Cache forensics + P-sweep instrumentation is patched from P ∈ {2, 5, 20, 50} to P ∈ {5, 20, 50} with the same rationale used in the ROADMAP SC4 edit"
    - "No touch to results/power/benchmark_batched.json; no touch to apply_decision_gate thresholds (STATE #105 — threshold tightening is a separate plan that consumes VERDICT.md)"
    - "git diff --stat src/ at phase close shows zero lines changed (diagnostic-only scope preserved per Phase 21 SC6)"
  artifacts:
    - path: ".planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md"
      provides: "Root-cause verdict + PR-level fix recommendation + follow-up plan naming"
      contains: "VERDICT|bottleneck|fix recommendation"
    - path: ".planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json"
      provides: "Final JSON: per_stage_compile_events + per_call_wall_clock + per_p_scaling + nuts_per_p_scaling + bottleneck_verdict (FILLED) + hlo_op_counts (FILLED)"
      contains: "bottleneck_layer"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 progress row + SC1/SC2/SC4 text brought into sync with CONTEXT.md"
      contains: "21 - Benchmark Bottleneck Diagnosis"
  key_links:
    - from: ".planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md"
      to: ".planning/phases/21-benchmark-bottleneck-diagnosis/benchmark-scope-memo.md"
      via: "cites HLO op counts + scan-body depth as scope evidence"
      pattern: "benchmark-scope-memo.md"
    - from: ".planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md"
      to: ".planning/phases/21-benchmark-bottleneck-diagnosis/cache_forensics.md"
      via: "cites HLO hash matrix + cache-dir listing as cache evidence"
      pattern: "cache_forensics.md"
    - from: ".planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md"
      to: ".planning/phases/14-integration-gpu-benchmark/14.1-03-PLAN.md"
      via: "names the fix that unblocks the Phase 14 benchmark resubmission"
      pattern: "14.1-03"
---

<objective>
Synthesize all Phase 21 evidence into a single verdict: name the one dominant
bottleneck causing the job-54899271 2197s-cold-1965s-warm-1.1x-speedup observation,
and specify a concrete PR-level fix recommendation that unblocks 14.1-03.

This is Wave 3 local desk work. All evidence — 21-01 scope memo, 21-03 Laplace
probe JSON, 21-04 NUTS probe JSON, 21-05 P-sweep CSV, 21-06 cache forensics memo —
is already on local. This plan reads them, adjudicates, and writes VERDICT.md.
In addition, this plan brings ROADMAP.md Phase 21 SC1/SC2/SC4 text into sync with
the CONTEXT.md-locked decisions (state-space-only baselines; VB-Laplace probe
filename + 7-value enum; P ∈ {5, 20, 50} with dropped-P=2 rationale; status
column replacing jit_wall_clock_s) before the phase is closed.

**In scope:**
- Read all Phase 21 artifacts:
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/benchmark-scope-memo.md`
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json`
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv`
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/cache_forensics.md`
    - Wave-2 SUMMARY files (21-02, 21-03, 21-04, 21-05) and 21-06 SUMMARY.
- Adjudicate among the candidate bottlenecks:
    1. NUTS sampler step function (leapfrog integrator, window adaptation,
       integration-steps unrolling).
    2. HGF scan graph size (scan-body depth + nested @jit decorators + Layer-2
       tree_map clamp + vmap over P).
    3. JIT cache key instability (closure-over-data → different HLO hash per
       call with different data — RESEARCH Risk #5).
    4. Cache initialization bug (JAX array created before cache env var set —
       RESEARCH §Known Pitfalls #6).
    5. Tracing-cost-dominates paradox (persistent cache HITS but tracing cost
       dwarfs XLA compile savings — JAX #22281).
    6. Shape polymorphism failure (Phase-1 N=2 cache never warms Phase-2 N=50 —
       structural by vmap design, per RESEARCH §"N=2 vs N=50 HLO Equivalence").
    7. Warmup_params confound (RESEARCH Risk #4): whether fit_batch_hierarchical
       accepts warmup_params per 21-02 Task 1 Patch C investigation, and if not,
       how much of the 1.1x speedup is explained by duplicate NUTS window
       adaptation. 21-02 SUMMARY records which branch was taken.
    8. Other (itemize anything the evidence surfaces that isn't on this list).
- Fill `vb_laplace_vs_nuts_jit.json`:
    - `bottleneck_verdict.bottleneck_layer` — set to the verdict token from CONTEXT-
      locked enum `{logp_graph, lbfgs_loop, hessian_graph, nuts_kernel, cache_key,
      both, undetermined}`. Pick ONE (or `both` if evidence genuinely splits).
    - `bottleneck_verdict.evidence` — a 2-3 paragraph narrative citing specific
      numbers (cold_jit_s at each P, cache_hit counts, HLO hash matches, HLO op
      counts, scan-body depth).
    - `hlo_op_counts` — populate from benchmark-scope-memo.md with keys for
      `logp_fn` (primary logp HLO), `scan_body` (per-trial inner loop),
      `laplace_hessian` (if 21-01 counted this; else leave empty with a note).
- Patch `.planning/ROADMAP.md` Phase 21 block (SC1/SC2/SC4 text) into sync with
  CONTEXT.md. See Task 2 Step 1 for the concrete edits.
- Patch `.planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md`
  §Cache forensics + P-sweep instrumentation to change `P ∈ {2, 5, 20, 50}` →
  `P ∈ {5, 20, 50}` with the same rationale used in the ROADMAP SC4 edit.
- Write `VERDICT.md` with the following mandatory sections:
    (a) Verdict: one sentence naming the single dominant bottleneck.
    (b) Evidence summary: for each of the 8 candidates above, a paragraph stating
        the supporting and refuting evidence with specific citations to artifacts.
    (c) PR-level fix recommendation: names file, function, patch shape.
        Example (not a prescription — the actual recommendation is evidence-driven):
          "Port `src/prl_hgf/fitting/hierarchical_patrl.py::build_logp_fn_batched_patrl`
          to the STATE #106/107 traced-args pattern used in
          `src/prl_hgf/fitting/hierarchical.py::_build_sample_loop` (lines 1304-1356).
          Specifically: refactor `logp_fn` to accept `state_arr`, `choices_arr`,
          `reward_mag_arr`, `shock_mag_arr`, `trial_mask`, `delta_hr_arr` as traced
          JIT function arguments rather than closure captures. This stabilizes the
          HLO hash across calls with different participant data and enables
          persistent-cache reuse across SLURM jobs."
    (d) Follow-up plan naming: identify the plan that implements the fix. Options:
          - 14.1-0X gap-closure (adds plan 14.1-07 or 14.1-08 after the existing
            14.1-01 through 14.1-06 chain)
          - New integer phase (e.g. 22-patrl-cache-fix)
          - A quick-NNN under .planning/quick/
        Pick one and justify.
    (e) Success criterion for the fix: a measurable test that proves the fix
        worked. Example:
          "After fix, re-run `cluster/21_diagnose_pscan.slurm --export=PROBE_P=50,
          PROBE_ROLE=warm` on M3. Expect: cold_jit_s / next_proc_warm_s > 3x at
          P=50. Expect: cold JIT at P=50 < 300s."
    (f) Unblock signal for 14.1-03: one-sentence note stating what 14.1-03 must do
        differently (or confirming that 14.1-03 is unblocked as-is once the fix is
        merged).
    (g) Deferred / out-of-scope items:
        - `apply_decision_gate` threshold tightening (STATE #105 — post-bench
          plan).
        - VB-Laplace as production fit-path fallback (CONTEXT.md deferred idea —
          only revive if 21-05 marked P=50 NUTS as `infeasible`).
    (h) Phase 21 closure checklist: all SCs 1-7 explicitly reviewed; state
        satisfied / partially / not-satisfied with a one-line justification each.

**Explicitly NOT in scope:**
- Implementing the fix. That's the follow-up plan named in section (d).
- Tightening `apply_decision_gate` thresholds (STATE #105, CONTEXT-banned until
  post-verdict).
- Resubmitting 14_benchmark_gpu.slurm. 14.1-03 owns that; VERDICT.md names the
  prerequisite.
- Any cluster work. All evidence is on local.
- Adding P=2 back to the P-sweep (B3: planner judgment keeps the ascending
  5→20→50 order; the ROADMAP+CONTEXT edits in Task 2 Step 1 formally record the
  dropped-P=2 rationale).

Purpose: Phase 21 exists to produce ONE document — VERDICT.md. Everything else is
evidence. This plan is the synthesis. The ROADMAP+CONTEXT edits in Task 2 Step 1
eliminate the SC1/SC2/SC4 text mismatches the checker flagged so that the phase
closes with all planning docs in sync.

Output:
- `.planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` with all 8
  mandatory sections.
- `.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json`
  with `bottleneck_verdict` and `hlo_op_counts` filled.
- `.planning/ROADMAP.md` Phase 21 block with SC1/SC2/SC4 text brought into sync
  with CONTEXT.md.
- `.planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md`
  §Cache forensics + P-sweep instrumentation updated to P ∈ {5, 20, 50}.
- `.planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md` recording
  the verdict headline and linking to the follow-up plan recommendation.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-RESEARCH.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/benchmark-scope-memo.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json
@.planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv
@.planning/phases/21-benchmark-bottleneck-diagnosis/cache_forensics.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-01-SUMMARY.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-02-SUMMARY.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-03-SUMMARY.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-04-SUMMARY.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-05-SUMMARY.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/21-06-SUMMARY.md
@.planning/phases/14-integration-gpu-benchmark/14.1-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all Phase 21 evidence and draft VERDICT.md</name>
  <files>
    .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md
  </files>
  <action>
  Local CPU desk work only.

  Step 1 — Systematic read of all evidence:
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/benchmark-scope-memo.md`
      → extract: HLO op count P=3 vs P=50 extrapolated; scan-body depth per model
      variant; structural-difference observations (nested @jit, tree_map clamp);
      comparison with state-space baselines.
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json`
      → extract: per_p_scaling (Laplace total_s at P=5,20); nuts_per_p_scaling
      (cold_s, warm_s at P=5,20); n_cache_hits and n_cache_misses per P + sampler;
      per_call_wall_clock if populated (21-02 B4 rename: cold/warm/next_proc wall
      times from time.perf_counter wrappers).
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv`
      → extract: for each P in {5, 20, 50} (where status=completed), the tuple
      (cold_jit_s, same_proc_warm_s, next_proc_warm_s). Compute ratios:
        - cold/same_proc_warm (in-process cache speedup)
        - cold/next_proc_warm (cross-process cache speedup)
        - same_proc_warm/next_proc_warm (tracing vs persistent cache comparison)
      Record the log-log slope of cold_jit_s vs P.
    - `.planning/phases/21-benchmark-bottleneck-diagnosis/cache_forensics.md`
      → extract: hash matrix rows; which of 21-06's three evidence-tracks is
      supported; cache-dir listing (if pasted via 21-06's checkpoint:human-action).
    - Wave-2 SUMMARY files for any observations the machine artifacts missed.
    - 21-02 SUMMARY → whether fit_batch_hierarchical accepts warmup_params
      (Candidate 7 evidence).

  Step 2 — Adjudicate candidates:
  For each of the 8 bottleneck candidates, write a paragraph evaluating evidence:

    1. NUTS sampler step function (leapfrog / warmup):
       Evidence for: NUTS cold_s / Laplace total_s ratio at matched P. If >3x,
       NUTS adds meaningful cost.
       Evidence against: If Laplace is ALSO slow (total_s > 60s at P=5), the
       scan body dominates regardless of sampler.
       Verdict bias: if NUTS/Laplace >> 1, support sampler; if NUTS/Laplace ~1,
       refute sampler.

    2. HGF scan graph size:
       Evidence for: 21-01 scope memo scan-body depth (~100-140 ops/trial vs
       NumPyro HMM ~200 but with no nested @jit — so pyhgf's compilation cost
       may be higher per trial due to nesting). HLO op count at P=50
       extrapolated >> state-space baselines.
       Evidence against: if same-proc in-process warm speedup is large (e.g.
       cold_s/same_proc_warm_s > 10x at P=5), the tracing cache works, meaning
       graph size itself is tractable once traced.
       Verdict bias: supported only if both (a) HLO op count is large AND (b)
       same-proc warm speedup is modest (<5x).

    3. JIT cache key instability (closure-over-data):
       Evidence for: next_proc_warm_s / cold_jit_s ratio close to 1.0 at ALL P
       (persistent cache never hits). HLO hash matrix from 21-06 shows
       pscan_P5_cold vs pscan_P5_warm (different SLURM processes) have
       DIFFERENT hashes. Matches RESEARCH Risk #5 pattern.
       Evidence against: HLO hashes identical across SLURM processes at the same
       P (pscan_P5_cold == pscan_P5_warm hash-wise).
       Verdict bias: this is the lead hypothesis per RESEARCH. Confirm with
       hash-matrix evidence.

    4. Cache initialization bug:
       Evidence for: cache-dir listing shows 0 or very few files after Wave-2
       jobs completed. Matches RESEARCH §Known Pitfalls #6.
       Evidence against: listing shows populated kernel binaries.
       Verdict bias: low prior (SLURM env var is set before Python starts per
       14_benchmark_gpu.slurm lines 17-50). Only supported if cache dir is
       genuinely empty after Wave 2.

    5. Tracing-cost-dominates paradox (JAX #22281):
       Evidence for: HLO hashes ARE stable across processes (= cache hits should
       fire) AND next_proc_warm_s is still slow (> 30% of cold_jit_s). Matches
       RESEARCH §"Cache Layers (Two-Layer System)".
       Evidence against: next_proc_warm_s << cold_jit_s (cache is saving time).
       Verdict bias: this is the fallback hypothesis if candidate #3 is refuted.

    6. Shape polymorphism failure (Phase-1 N=2 not warming N=50):
       Evidence for: pscan_P5_cold vs pscan_P50_cold HLO hashes are different
       (expected by vmap) AND Phase-1 of 14_benchmark_gpu.slurm uses N=2 while
       Phase-2 uses N=50.
       Evidence against: pscan_P5_cold == pscan_P50_cold (would mean vmap is
       shape-polymorphic, which would be positive news but is unexpected).
       Verdict bias: this IS the Phase-1-vs-Phase-2 structural issue. This is
       NOT a "bug" per se — it's the bench design being wrong. This bottleneck
       has a clean fix: rewrite Phase 1 to run at P=50 directly.

    7. Warmup_params duplication confound (RESEARCH Risk #4):
       Evidence for: 21-02 SUMMARY records whether fit_batch_hierarchical accepts
       warmup_params. If it does NOT, the warm call re-runs NUTS window
       adaptation, adding ~1100s to the warm wall-clock — partially explaining
       the 1.1x observation without invoking cache-key or graph-size hypotheses.
       Evidence against: 21-02 confirmed the kwarg exists and the warm call uses
       it; the 1.1x must then be explained by some other mechanism.
       Verdict bias: this is a CONFOUND, not a primary verdict candidate. Must
       be flagged in VERDICT.md regardless of the chosen verdict so the reader
       understands what fraction of the 1965s warm time is explainable by
       duplicate warm-up vs true cache failure.

    8. Other / combination:
       - "both": if Laplace AND NUTS show the same HLO-hash instability pattern,
         the fix is cache-key-level; if Laplace is fast and NUTS is slow, the
         sampler needs attention too.

  Step 3 — Pick the verdict.
  Based on the weight of evidence, pick ONE (or explicitly "both X and Y").
  Per RESEARCH §"Risks", the most likely verdict is closure-over-data (Candidate
  3) — but this plan must confirm with actual numbers, not repeat the hypothesis.

  If the evidence is genuinely ambiguous (e.g., pscan_P5_cold vs pscan_P5_warm
  hashes are the same AND next_proc_warm_s < cold_jit_s), revisit the data before
  calling "undetermined" — undetermined is an acceptable answer ONLY if the
  evidence truly does not discriminate. In that case, name the follow-up experiment
  needed (e.g., a finer-grained split of the LBFGS vs Hessian compile timings via
  the deferred Area-3 harness from CONTEXT.md).

  Step 4 — Write `VERDICT.md` with the 8 mandatory sections from the objective
  (keep it under ~500 lines; prose-heavy but not padded):

    Header:
    ```markdown
    # Phase 21 VERDICT: Benchmark Bottleneck Root Cause

    **Phase:** 21-benchmark-bottleneck-diagnosis
    **Dated:** <today>
    **Synthesized from:** 21-01 scope memo, 21-02 harness SUMMARY, 21-03 Laplace
    probe, 21-04 NUTS probe, 21-05 P-sweep, 21-06 cache forensics

    ---

    ## (a) Verdict

    The single dominant bottleneck causing job-54899271's 2197s-cold / 1965s-warm
    / 1.1x-speedup observation is: **<VERDICT_TOKEN>**.

    [One sentence narrative.]

    ---

    ## (b) Evidence Summary by Candidate

    ### Candidate 1: NUTS sampler step function
    [Evidence for:]
    [Evidence against:]
    [Verdict on this candidate: supported | partially supported | refuted]

    ### Candidate 2: HGF scan graph size
    [same structure]

    ### Candidate 3: JIT cache key instability (closure-over-data)
    [same structure]

    ### Candidate 4: Cache initialization bug
    [same structure]

    ### Candidate 5: Tracing-cost paradox (JAX #22281)
    [same structure]

    ### Candidate 6: Shape polymorphism failure (Phase-1 N=2 vs Phase-2 N=50)
    [same structure]

    ### Candidate 7: Warmup_params duplication confound (RESEARCH Risk #4)
    [same structure, plus a specific statement on what fraction of the
    1965s warm call is explained by duplicate NUTS window adaptation,
    per 21-02 SUMMARY's finding on fit_batch_hierarchical warmup_params support]

    ### Candidate 8: Other / combination
    [same structure]

    ---

    ## (c) PR-Level Fix Recommendation

    **File:** src/prl_hgf/fitting/hierarchical_patrl.py
    **Function:** build_logp_fn_batched_patrl
    **Patch shape:** [concrete description — see example in objective]

    [Or other file/function/patch depending on the verdict.]

    ---

    ## (d) Follow-up Plan

    Recommended: **<plan number / phase number / quick task>**.

    Justification: [why this scope — not a gap-closure if the fix is non-trivial;
    not a new phase if the fix is a surgical patch; quick task only for trivial
    fixes.]

    ---

    ## (e) Success Criterion for the Fix

    **Test:** [concrete measurable invariant]
    **Expected outcome:** [specific numerical target]
    **Command to run:** [executable command]

    ---

    ## (f) 14.1-03 Unblock Signal

    [One sentence: either "14.1-03 can proceed as-is once the fix is merged to
    main" OR "14.1-03 must be modified to <...> — pending a separate plan update
    alongside the fix."]

    ---

    ## (g) Deferred / Out-of-Scope

    - `apply_decision_gate` threshold tightening: deferred per STATE #105; a
      separate plan consumes this VERDICT's numbers to tighten thresholds.
    - VB-Laplace as production fit-path fallback: [revive if P=50 NUTS was
      marked infeasible in 21-05; otherwise leave as backlog note].
    - [anything else evidence surfaced but is out of Phase 21 scope]

    ---

    ## (h) Phase 21 SC Closure Checklist

    | SC | Status | Evidence |
    |----|--------|----------|
    | SC1 — graph scope memo (state-space baselines) | satisfied | benchmark-scope-memo.md + 21-01 summary |
    | SC2 — VB-Laplace-vs-NUTS JIT (vb_laplace_vs_nuts_jit.json, 7-value enum) | satisfied | vb_laplace_vs_nuts_jit.json (4 top-level keys; per_stage_compile_events + per_call_wall_clock populated per 21-02 B4 rename) |
    | SC3 — cache forensics | satisfied | cache_forensics.md (all three sub-questions + 1.1x narrative) |
    | SC4 — P-sweep (P ∈ {5, 20, 50}) | [satisfied / partial — note early-stopped rows] | jit_scaling_sweep.csv |
    | SC5 — root-cause verdict | satisfied | this document |
    | SC6 — diagnostic-only scope | satisfied | `git diff --stat src/` == 0 lines |
    | SC7 — fast feedback loop | satisfied | max walltime used = <X>min across all SLURM jobs |
    ```

  Step 5 — Fill vb_laplace_vs_nuts_jit.json:
  Load the JSON. Set:
    - `bottleneck_verdict.bottleneck_layer` = the verdict token (must be in the
      CONTEXT-locked enum `{logp_graph, lbfgs_loop, hessian_graph, nuts_kernel,
      cache_key, both, undetermined}`).
    - `bottleneck_verdict.evidence` = a 2-3 paragraph string matching VERDICT.md
      section (a)+(b) highlights.
    - `hlo_op_counts` — copy from benchmark-scope-memo.md's comparison table:
      ```
      "hlo_op_counts": {
          "patrl_3level_logp_fn_P3": <int>,
          "patrl_3level_logp_fn_P50_extrapolated": <int>,
          "patrl_3level_scan_body_per_trial": <int>,
          "patrl_2level_scan_body_per_trial": <int>,
          "pick_best_cue_3level_scan_body_per_trial": <int>,
          "notes": "Derived from 21-01 benchmark-scope-memo.md; ~2x tolerance; NUTS-step-count assumption documented there."
      }
      ```
  Write the JSON back with `json.dumps(data, indent=2)`.

  Step 6 — DO NOT touch apply_decision_gate thresholds. DO NOT resubmit
  14_benchmark_gpu.slurm. DO NOT modify production code.
  </action>
  <verify>
  1. `ls .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` exits 0.
  2. `grep -c '## (a) Verdict' .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` == 1.
  3. `grep -cE '## \(b\)|## \(c\)|## \(d\)|## \(e\)|## \(f\)|## \(g\)|## \(h\)' .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` == 7 (all 7 remaining mandatory sections present).
  4. `grep -c 'src/prl_hgf/' .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` >= 1 (PR-level fix cites a source file path).
  5. `grep -c 'SC1' .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` >= 1 (SC closure table present).
  6. `python -c "import json; d = json.load(open('.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json')); assert d['bottleneck_verdict']['bottleneck_layer'] != 'undetermined' or 'inconclusive' in d['bottleneck_verdict']['evidence'].lower()"` exits 0 (verdict is filled OR documents inconclusiveness).
  7. `python -c "import json; d = json.load(open('.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json')); assert d['hlo_op_counts']"` exits 0 (hlo_op_counts no longer empty).
  8. `grep -c '14.1-03' .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` >= 1 (14.1-03 unblock signal present).
  9. `grep -c 'Candidate 7' .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` >= 1 (warmup_params confound addressed, per 21-02 SUMMARY dependency).
  10. `git diff --stat src/ | grep -c 'changed'` == 0 (diagnostic-only scope preserved).
  </verify>
  <done>
  VERDICT.md written with all 8 mandatory sections (including Candidate 7
  warmup_params confound citing 21-02 SUMMARY); JSON verdict field filled;
  hlo_op_counts filled; no production code changed; 14.1-03 unblock signal present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the phase — sync ROADMAP/CONTEXT text, update STATE, and commit</name>
  <files>
    .planning/ROADMAP.md
    .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md
    .planning/STATE.md
    .planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md
  </files>
  <action>
  Step 1 — Patch `.planning/ROADMAP.md` Phase 21 block so SC1/SC2/SC4 text matches
  the CONTEXT-locked decisions, then update the progress row. Do all of the
  following surgical edits in one pass (use Edit-style targeted replacements; do
  NOT rewrite the entire Phase 21 block):

  **Edit 1.1 — SC1 text rewrite (ROADMAP line ~348).**
  Per CONTEXT.md §Comparison baselines, state-space models are the only allowed
  comparison corpus; Stan/NumPyro 8-schools, Radon-hierarchical, BNN ~1M params,
  transformer forward pass, and standard CNN forward pass are all BANNED.

  Find the SC1 text block that currently reads (quote approximate — grep for the
  distinctive phrase "against at minimum: Stan/NumPyro 8-schools"):
    `..., against at minimum: Stan/NumPyro 8-schools, Radon-hierarchical, a BNN
    with ~1M params, a transformer forward pass (ballpark), and a standard CNN
    forward pass (ballpark); ...`
  Replace with:
    `..., against state-space model baselines (NumPyro HMMs, Kalman-filter
    scan-based MCMC fits, LGSSM examples) — closest structural match to HGF's
    per-trial scan-in-logp, per CONTEXT.md §Comparison baselines; ...`
  Remove the 8-schools / Radon / BNN / transformer / CNN list entirely.

  **Edit 1.2 — SC2 text rewrite (ROADMAP line ~349).**
  Per CONTEXT.md §VB-Laplace probe design, the filename is
  `vb_laplace_vs_nuts_jit.json` (not `svi_vs_nuts_jit.json`); the enum is the
  7-value `{logp_graph, lbfgs_loop, hessian_graph, nuts_kernel, cache_key, both,
  undetermined}` (not the 4-value `{logp_graph, nuts_sampler, both,
  undetermined}`); and the probe is VB-Laplace (reusing
  `fit_vb_laplace_patrl` from Phase 19), not `numpyro.infer.SVI` with an
  AutoNormal guide.

  Find the SC2 text block that currently contains `svi_vs_nuts_jit.json`.
  Rewrite the whole SC2 paragraph to:
    `**VB-Laplace vs NUTS JIT comparison** at
    \`.planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json\`
    records cold and warm JIT compile events and per-call wall-clock times for
    VB-Laplace (reusing \`fit_vb_laplace_patrl\` from Phase 19 — the already-
    validated TAPAS-parity quasi-Newton MAP + \`jax.hessian\` Laplace covariance
    path; zero new deps) and \`blackjax.nuts\` (window adaptation, ≈10 draws) on
    the **identical** \`_build_patrl_log_posterior\` closure at matched shapes.
    Diagnostic rule: if VB-Laplace compiles slow (>5 min cold on GPU), bottleneck
    is the logp graph (scan body, vmap shape, closure over pyhgf Network); if
    VB-Laplace compiles fast and NUTS still slow, bottleneck is the NUTS step
    function. Verdict recorded in the JSON under \`"bottleneck_verdict.bottleneck_layer"\`
    with values in \`{"logp_graph", "lbfgs_loop", "hessian_graph", "nuts_kernel",
    "cache_key", "both", "undetermined"}\`. Per CONTEXT.md §VB-Laplace probe
    design, reusing the Phase 19 validated Laplace path replaces the originally-
    proposed SVI probe to avoid introducing numpyro.SVI as a new diagnostic dep.`

  Also update the "SVI probe" phrase in the Phase 21 goal sentence (line ~341):
    `a SVI-vs-NUTS JIT comparison that isolates` →
    `a VB-Laplace-vs-NUTS JIT comparison that isolates`
  And in the Phase 21 section header (line 339):
    `Phase 21: Benchmark Bottleneck Diagnosis — Graph Scope + SVI Probe + JIT Cache Forensics` →
    `Phase 21: Benchmark Bottleneck Diagnosis — Graph Scope + VB-Laplace Probe + JIT Cache Forensics`
  And in "Sources of record" (line ~373) — remove or replace the line that reads:
    `- \`numpyro.infer.SVI\` + \`numpyro.infer.autoguide.AutoNormal\` — the non-MCMC reference fit for the SVI probe (SC2)`
  with:
    `- \`src/prl_hgf/fitting/fit_vb_laplace_patrl.py::fit_vb_laplace_patrl\` — the VB-Laplace reference fit for the logp-graph-vs-sampler isolation probe (SC2; Phase 19 TAPAS-parity path; zero new deps)`
  And in Architectural directives (line ~385):
    `- **Isolate before fixing.** SVI probe must land before any production-sampler code change. If SVI compiles fast, ...` →
    `- **Isolate before fixing.** VB-Laplace probe must land before any production-sampler code change. If VB-Laplace compiles fast, ...` (preserve the rest of the bullet's logic verbatim).

  **Edit 1.3 — SC4 text rewrite (ROADMAP line ~351).**
  Per B3 / CONTEXT.md decision and M1 planner judgment: P ∈ {5, 20, 50} (P=2
  dropped); column list ends with `status` (not `jit_wall_clock_s`).

  Find the SC4 text block containing `P ∈ {2, 5, 20, 50}` and the column list
  `P, cold_jit_s, same_proc_warm_s, next_proc_warm_s, draws_per_s, vram_peak_mb,
  jit_wall_clock_s`. Rewrite to:
    `**P-sweep JIT scaling** at
    \`.planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv\`
    with one row per P ∈ {5, 20, 50} (or a subset if the L40S hits VRAM limits
    sooner), columns
    \`P, cold_jit_s, same_proc_warm_s, next_proc_warm_s, draws_per_s,
    vram_peak_mb, status\` where \`status ∈ {completed, early_stopped,
    infeasible}\`. **P=2 dropped: too small to be informative per planner
    judgment; CONTEXT 'iterate by increasing' directive honored by ascending
    5 → 20 → 50. Status column replaced wall-clock-duplicate per planner
    judgment; early-stop visibility more valuable than redundant timing.** Each
    P runs as a **separate short-walltime SLURM job** (≤30 min) using the
    existing \`14_benchmark_gpu.slurm\` infrastructure with a new \`--p-scan P\`
    CLI that forces a single-P run and writes one CSV row. Plot \`log(jit_s)\`
    vs \`log(P)\` — linear slope ≈ 1 is acceptable trace-time scaling; slope
    ≫ 1 or super-exponential indicates a graph blow-up to fix. \`next_proc_warm_s\`
    (second SLURM job same shape, same cache dir) is the authoritative persistent-
    cache measurement; \`same_proc_warm_s\` (second call within one Python
    process) is the in-process JIT trace cache measurement. If
    \`next_proc_warm_s > same_proc_warm_s × 2\`, the persistent cache is not
    landing.`

  Also update the scope-notes bullet on cluster-vs-local split (line ~380) if it
  references SVI:
    `(b) cluster SVI probe + cache forensics next,` →
    `(b) cluster VB-Laplace + NUTS probes + cache forensics next,`

  **Edit 1.4 — Progress row update (ROADMAP line ~414).**
  Current:
    `| 21 - Benchmark Bottleneck Diagnosis | v1.2 | 0/0 | Planned | -- |`
  Replace with:
    `| 21 - Benchmark Bottleneck Diagnosis | v1.2 | 7/7 | Complete | <today's date YYYY-MM-DD> |`

  **Edit 1.5 — Phase 21 Plans list update (ROADMAP lines ~358-365).**
  Mark all 7 plans `[x]` (they are complete at this point in the execution).

  Step 2 — Patch `.planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md`
  §Cache forensics + P-sweep instrumentation (line ~55):
  Find the P-sweep cluster job structure bullet that currently reads:
    `- **P-sweep cluster job structure**: SLURM array with \`--dependency=afterok\` chain. For each P ∈ {2, 5, 20, 50}: job A cold (populates cache), job B next-proc warm (reads cache). ...`
  Replace `P ∈ {2, 5, 20, 50}` with `P ∈ {5, 20, 50}` and append to the same
  bullet (after the existing sentence about ≤30min walltime):
    ` **P=2 dropped: too small to be informative per planner judgment; CONTEXT
    'iterate by increasing' directive honored by ascending 5 → 20 → 50.**`

  Also patch the VB-Laplace probe design decisions bullet list (line ~43-47)
  for B4 (JAX_LOG_COMPILES timing rename). Find the \`per_stage_compile_times\`
  bullet that currently reads:
    `- \`per_stage_compile_times\` — cold + warm per kernel (LBFGS body, Hessian, NUTS step), sourced from JAX_LOG_COMPILES log parsing`
  Replace with (note the rename plus the new per_call_wall_clock sibling):
    `- \`per_stage_compile_events\` — cold + warm event counts per kernel
    (LBFGS body, Hessian, NUTS step), sourced from JAX_LOG_COMPILES log parsing.
    **Note**: JAX_LOG_COMPILES does NOT emit per-event wall-clock timing;
    per-stage timing would require harness-splitting which this section
    explicitly rejects (full-pipeline-only). Field was originally
    \`per_stage_compile_times\` but was renamed to \`per_stage_compile_events\`
    when B4 verification found JAX_LOG_COMPILES emits event counts, not timings.`
    `- \`per_call_wall_clock\` — \`{cold_s, warm_s, next_proc_s}\` populated from
    \`time.perf_counter()\` wrappers around the full-pipeline \`fit_vb_laplace_patrl\`
    calls (first in-process cold, second in-process warm, third subprocess
    next-proc). Sibling to \`per_stage_compile_events\`; full-pipeline wall-clock
    is the timing signal when per-stage timing is not emit-able.`

  Also update the §Comparison baselines + FLOPs methodology bullet list if
  needed: it already says state-space only, so no edit needed there — only the
  ROADMAP needed the B1 sync.

  Also update the \`vb_laplace_vs_nuts_jit.json\` fields summary line:
    `- \`vb_laplace_vs_nuts_jit.json\` fields** (all four required):` →
    `- \`vb_laplace_vs_nuts_jit.json\` fields** (all five required):`
  (bumping from 4 to 5 because per_call_wall_clock is a new sibling).

  Step 3 — Update `.planning/STATE.md`:
  Find the "Current Position" block (around line 10-14). Update:
    - `Phase:` → `21 (Benchmark Bottleneck Diagnosis) — COMPLETE`
    - `Plan:` → `21-07 COMPLETE (all 7/7 plans complete — 21-01 through 21-07)`
    - `Status:` → `VERDICT.md published; follow-up plan <named plan> unblocks 14.1-03`
    - `Last activity:` → `<today> — Published VERDICT.md naming <VERDICT_TOKEN> as root cause`

  Append a decision row to the Key Decisions table (Phase column: `21`):
    - Decision: `Phase 21 diagnostic verdict: <one-sentence verdict>`
    - Rationale: `Evidence: <key number, e.g. "next_proc_warm_s/cold_jit_s = 0.9 at P=50 with HLO hash instability"> — see VERDICT.md`

  Step 4 — Write
  `.planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md`:
    (a) Verdict headline (1-2 sentences).
    (b) Follow-up plan name + scope.
    (c) 14.1-03 unblock status.
    (d) SC closure summary (table).
    (e) Pointer to VERDICT.md.
    (f) Note on ROADMAP+CONTEXT sync edits (SC1 state-space baselines, SC2
        vb_laplace_vs_nuts_jit.json + 7-value enum, SC4 P ∈ {5, 20, 50} +
        status column) confirming the checker's B1/B2/B3/M1 blockers are
        resolved at phase close.

  Step 5 — Git commit all Phase 21 closure artifacts:
    ```bash
    git add .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md \
            .planning/phases/21-benchmark-bottleneck-diagnosis/vb_laplace_vs_nuts_jit.json \
            .planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md \
            .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md \
            .planning/ROADMAP.md \
            .planning/STATE.md
    git commit -m "docs(21-07): publish VERDICT.md + sync ROADMAP/CONTEXT + close Phase 21"
    git push origin main
    ```
  The push is important: the cluster-side repo needs the updated STATE.md and
  VERDICT.md to avoid re-running Phase 21 work during any future `/gsd:*` invocations.
  </action>
  <verify>
  1. `grep -c '21 - Benchmark Bottleneck Diagnosis.*7/7.*Complete' .planning/ROADMAP.md` == 1.
  2. `grep -cE '8-schools|Radon-hierarchical|BNN with ~1M|transformer forward pass|standard CNN forward pass' .planning/ROADMAP.md` == 0 (Phase 21 SC1 no longer lists banned baselines).
  3. `grep -c 'state-space model baselines' .planning/ROADMAP.md` >= 1 (Phase 21 SC1 rewritten per CONTEXT §Comparison baselines).
  4. `grep -c 'svi_vs_nuts_jit.json' .planning/ROADMAP.md` == 0 (old filename retired).
  5. `grep -c 'vb_laplace_vs_nuts_jit.json' .planning/ROADMAP.md` >= 1 (new filename present).
  6. `grep -cE 'lbfgs_loop|hessian_graph|nuts_kernel|cache_key' .planning/ROADMAP.md` >= 4 (7-value enum present in SC2 rewrite).
  7. `grep -cE 'P \\\\in \\\\{2,' .planning/ROADMAP.md` == 0 (P=2 removed from SC4; careful with LaTeX escaping — adjust grep as needed).
  8. `grep -cE 'P \\\\in \\\\{5, 20, 50\\\\}' .planning/ROADMAP.md` >= 1 (new P-sweep set in SC4).
  9. `grep -c 'jit_wall_clock_s' .planning/ROADMAP.md` == 0 (M1: column retired).
  10. `grep -cE 'status.*\\{completed, early_stopped, infeasible\\}' .planning/ROADMAP.md` >= 1 (status enum present).
  11. `grep -c 'P ∈ {2,' .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md` == 0 (CONTEXT.md updated).
  12. `grep -c 'P ∈ {5, 20, 50}' .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md` >= 1.
  13. `grep -c 'per_stage_compile_events' .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md` >= 1 (B4 rename landed in CONTEXT).
  14. `grep -c 'per_call_wall_clock' .planning/phases/21-benchmark-bottleneck-diagnosis/21-CONTEXT.md` >= 1 (B4 sibling field landed in CONTEXT).
  15. `grep -c 'Phase 21.*COMPLETE' .planning/STATE.md` >= 1.
  16. `ls .planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md` exits 0.
  17. `git log --oneline -1 -- .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md` shows `docs(21-07)` commit.
  18. `git log origin/main --oneline -1` matches the local `git log --oneline -1` (push succeeded).
  19. `git diff --stat src/` shows zero lines changed (diagnostic-only scope preserved per SC6).
  20. `git diff --stat results/` shows zero lines changed (14.1-03's artifacts untouched).
  </verify>
  <done>
  ROADMAP SC1/SC2/SC4 text synced with CONTEXT.md (B1/B2/B3/M1 resolved); CONTEXT.md
  P-sweep set + per_stage_compile_events/per_call_wall_clock updated (B3/B4
  resolved); STATE + 21-07 SUMMARY recorded; VERDICT.md published; Phase 21 closed
  and pushed to origin/main; production code untouched.
  </done>
</task>

</tasks>

<verification>
1. VERDICT.md has all 8 mandatory sections (a-h) including PR-level fix recommendation with concrete file + function + patch shape and Candidate 7 warmup_params confound citing 21-02 SUMMARY.
2. vb_laplace_vs_nuts_jit.json has bottleneck_verdict.bottleneck_layer filled (not "undetermined" unless genuinely inconclusive) and hlo_op_counts populated.
3. ROADMAP Phase 21 SC1 lists state-space baselines only (8-schools/Radon/BNN/transformer/CNN all removed).
4. ROADMAP Phase 21 SC2 uses vb_laplace_vs_nuts_jit.json filename + 7-value enum + VB-Laplace probe wording.
5. ROADMAP Phase 21 SC4 uses P ∈ {5, 20, 50} with status column (jit_wall_clock_s retired) and dropped-P=2 rationale.
6. CONTEXT.md P-sweep instrumentation updated to P ∈ {5, 20, 50} + per_stage_compile_events rename + per_call_wall_clock sibling.
7. Phase 21 SC5 satisfied: root-cause verdict + fix recommendation exist.
8. Phase 21 SC6 satisfied: `git diff --stat src/` shows 0 lines changed; `git diff --stat results/` shows 0 lines; apply_decision_gate thresholds untouched.
9. 14.1-03 unblock signal present in VERDICT.md.
10. ROADMAP progress row + STATE updated; phase closed.
</verification>

<success_criteria>
- Phase 21 SC5 satisfied: VERDICT.md names the single dominant bottleneck with
  cited evidence from SCs 1-4 and specifies a concrete PR-level fix.
- Phase 21 SC6 satisfied throughout entire phase: `git diff --stat src/` == 0;
  `git diff --stat results/` == 0; `apply_decision_gate` thresholds unchanged;
  new code confined to scripts/08_run_power_iteration.py diagnostic helpers,
  cluster/21_*.slurm + .sh files, and .planning/phases/21-*/* artifacts.
- ROADMAP + CONTEXT are in sync with each other and with the actual artifacts
  produced by plans 21-01 through 21-06: SC1 state-space baselines; SC2
  vb_laplace_vs_nuts_jit.json + 7-value enum + VB-Laplace probe; SC4 P ∈
  {5, 20, 50} + status column.
- Phase 21 complete (7 plans, 3 waves) with all 5 required artifacts (benchmark-scope-memo.md,
  vb_laplace_vs_nuts_jit.json, jit_scaling_sweep.csv, cache_forensics.md, VERDICT.md).
- 14.1-03 knows whether it can proceed as-is or needs an intervening fix plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-benchmark-bottleneck-diagnosis/21-07-SUMMARY.md`
with: (a) verdict headline (1-2 sentences naming the bottleneck), (b) named
follow-up plan (14.1-07 / 22-... / quick-NNN) + scope, (c) 14.1-03 unblock status
(unblocked-as-is vs must-wait-for-fix), (d) SC 1-7 closure summary table
(satisfied/partial/not-satisfied per SC), (e) pointer to VERDICT.md + to all five
required artifacts, (f) note on ROADMAP+CONTEXT sync edits (B1 state-space-only
baselines, B2 vb_laplace_vs_nuts_jit.json + 7-value enum + VB-Laplace probe, B3
P ∈ {5, 20, 50} + dropped-P=2 rationale, B4 per_stage_compile_events rename +
per_call_wall_clock sibling, M1 status column replacing jit_wall_clock_s) with a
one-line cross-reference back to this plan, (g) git log line showing the Phase
21 closure commit is on origin/main.
</output>

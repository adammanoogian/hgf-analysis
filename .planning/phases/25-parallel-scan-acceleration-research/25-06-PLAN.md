---
phase: 25-parallel-scan-acceleration-research
plan: "06"
type: execute
wave: 7
depends_on:
  - "25-05"
files_modified: []
files_created:
  - .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md
  - .planning/phases/25-parallel-scan-acceleration-research/25-06-SUMMARY.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "PRODUCTIONIZATION_VERDICT.md synthesizes 25-00 (RESEARCH.md), 25-00b (API_BRIDGING_STUDY.md), 25-01 (THEORETICAL_VALIDATION.md), 25-02 (ALGORITHM_DECISION.md), 25-03 (REPRODUCTION_NOTES.md), 25-04 (HGF_PROTOTYPE_NOTES.md), 25-05 (RECOVERY_COMPARISON.md) into a single GO / NO-GO / CONDITIONAL-GO verdict"
    - "Verdict explicitly distinguishes the two production scenarios surfaced in 25-RESEARCH.md §1 and §9: (a) drop-in replacement at T=420 production scale (LOW value, marginal speedup) vs (b) enabler for T>>420 / branched HGF / trial-varying theta phases (HIGH value, large speedup)"
    - "Speedup at production shape (P=300, T=420) is reported with a specific number from 25-04 results/diagnostics/hgf_pscan_proto.json"
    - "Speedup at T>>420 (e.g. P=50, T=2000) is reported with a specific number from same JSON"
    - "Phase 14.2 variants 1-5 baseline speedups are cited so the reader can compare: does parallel scan exceed Phase 14.2 wins on the non-trivial cases (branched, trial-varying, longer T)?"
    - "User-facing API change is described: if GO, what does the production fitting code change look like? (e.g. new flag like fit_method='nuts_parallel' alongside existing 'nuts' and 'vb_laplace')"
    - "Recommended next phase is named: if GO, the productionization phase number and scope; if NO-GO, the conditions under which to revisit; if CONDITIONAL-GO, the specific conditions (e.g. only for new T>>420 phases, not retroactive)"
    - "Risk register documents: maintenance burden (PIEKS/ELK reference impl as dependency), JAX-version compatibility, pyhgf upstream changes risk, gradient-through-iteration brittleness"
    - "User-facing API change is described for hierarchical.py (e.g. new flag fit_method='nuts_parallel' alongside 'nuts' and 'vb_laplace'); explicitly notes that hierarchical_patrl.py / fit_vb_laplace_patrl.py would require parallel changes in the productionization phase (out of scope for Phase 25)"
  artifacts:
    - path: ".planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md"
      provides: "Phase 25 closing memo: GO/NO-GO/CONDITIONAL-GO verdict with synthesis of all sub-phases and recommended next phase"
      contains: "GO"
  key_links:
    - from: ".planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md"
      to: "results/diagnostics/hgf_pscan_proto.json"
      via: "Cites speedup numbers at three (P, T) shapes from 25-04 timing benchmark"
      pattern: "speedup|hgf_pscan_proto"
    - from: ".planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md"
      to: ".planning/phases/25-parallel-scan-acceleration-research/RECOVERY_COMPARISON.md"
      via: "Cites recovery RMSE/ESS verdict from 25-05"
      pattern: "RMSE|RECOVERY_COMPARISON"
    - from: ".planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md"
      to: ".planning/ROADMAP.md"
      via: "Recommended next phase number / scope feeds into ROADMAP planning"
      pattern: "ROADMAP|next phase|v1.4"
    - from: ".planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md"
      to: ".planning/phases/14-2-variant-comparison/results/"
      via: "Cites Phase 14.2 variants 1-5 timing baseline for comparison against parallel-scan speedups"
      pattern: "Phase 14.2|variant"
---

<objective>
Produce `PRODUCTIONIZATION_VERDICT.md`: synthesize all Phase 25 sub-phases into
a single GO / NO-GO / CONDITIONAL-GO verdict on whether to productionize the
parallel-scan HGF likelihood, and what the next phase looks like.

This is a desk-synthesis plan; no code, no compute. The verdict cites:
- 25-RESEARCH.md (literature synthesis)
- 25-00b API_BRIDGING_STUDY.md (engineering feasibility)
- 25-01 THEORETICAL_VALIDATION.md (lambda_hat distribution, K prediction)
- 25-02 ALGORITHM_DECISION.md (chosen algorithm, rationale)
- 25-03 REPRODUCTION_NOTES.md (reference impl reproduces published numbers)
- 25-04 HGF_PROTOTYPE_NOTES.md (numerical agreement, GPU timing)
- 25-05 RECOVERY_COMPARISON.md (statistical equivalence vs sequential)

The verdict must explicitly distinguish two production scenarios:

**Scenario A: Drop-in replacement at T=420 production (current pick_best_cue
and PAT-RL).** Per 25-RESEARCH.md §1 and §9, T=420 is in the marginal/breakeven
zone; speedup is 0.5-2x. Phase 14.2 variants 1-5 already attack production-
scale runtime through other mechanisms; parallel-scan would be a small win at
best at this scale.

**Scenario B: Enabler for T>>420 phases.** The Phase 25 motivation per
PHASE_DESCRIPTION.md is "scaling HGF to T >> 420 (longitudinal / continuous-
time / 4-level HGF), branched/multimodal HGFs, trial-varying parameters, online
inference, and population-level hierarchical pooling." For these, parallel
scan is in the win zone (5-20x). These phases don't exist yet but are foreseen.

The verdict must say what's true for EACH scenario and propose a recommended
next phase accordingly.

**PRECONDITION:** Check that Phase 14.2 cluster runs have landed
(`ls results/diagnostics/variant_comparison.csv` exists and is populated)
before writing Section 3 of the verdict memo. The Section 3 production-shape
speedup analysis cites Phase 14.2 variants 1-5 sequential-baseline numbers as
the production reference; if Phase 14.2 has not produced its CSV yet, the
verdict memo cannot quantify the comparison and 25-06 should pause until the
Phase 14.2 baseline is on disk.

**In scope:**
- Synthesis memo PRODUCTIONIZATION_VERDICT.md.
- GO / NO-GO / CONDITIONAL-GO verdict.
- Recommended next-phase scope (if GO or CONDITIONAL-GO).
- Risk register for productionization.
- API change description (if GO).

**Explicitly NOT in scope:**
- Implementing the productionization (that's the recommended-next-phase work).
- Modifying ROADMAP.md (the planner does that as a side-effect of the
  /gsd:plan-phase orchestrator step; this plan only NAMES the recommended
  next phase).
- Re-running any experiments.

**Why autonomous: false.** The verdict is a research/product judgment call
that the user should explicitly weigh in on. CONDITIONAL-GO with specific
conditions is the most likely outcome; the conditions need user buy-in.

Effort: 2 days research-pace (per PHASE_DESCRIPTION.md). Pure synthesis work.

Output:
- `.planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md`
- `.planning/phases/25-parallel-scan-acceleration-research/25-06-SUMMARY.md`
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-parallel-scan-acceleration-research/PHASE_DESCRIPTION.md
@.planning/phases/25-parallel-scan-acceleration-research/25-RESEARCH.md
@.planning/phases/25-parallel-scan-acceleration-research/API_BRIDGING_STUDY.md
@.planning/phases/25-parallel-scan-acceleration-research/THEORETICAL_VALIDATION.md
@.planning/phases/25-parallel-scan-acceleration-research/ALGORITHM_DECISION.md
@.planning/phases/25-parallel-scan-acceleration-research/REPRODUCTION_NOTES.md
@.planning/phases/25-parallel-scan-acceleration-research/HGF_PROTOTYPE_NOTES.md
@.planning/phases/25-parallel-scan-acceleration-research/RECOVERY_COMPARISON.md
@results/diagnostics/hgf_pscan_proto.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PRODUCTIONIZATION_VERDICT.md synthesizing all sub-phases</name>
  <files>
    .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md
  </files>
  <action>
  Pure desk synthesis. Write the closing memo for Phase 25.

  Required sections:

  (1) Executive summary (5-7 bullets):
    - Verdict: GO / NO-GO / CONDITIONAL-GO.
    - Algorithm chosen (from 25-02): ___.
    - Theoretical validation (from 25-01): lambda_hat median = X (PASS / FAIL).
    - Reference reproduction (from 25-03): rel-err = Y, K matched paper claim.
    - HGF prototype numerical agreement (from 25-04): rel-err < 1e-5 confirmed.
    - HGF prototype GPU speedup at production shape (P=300, T=420): Z x.
    - HGF prototype GPU speedup at T=2000: A x.
    - Recovery comparison (from 25-05): RMSE / ESS within tolerance, B
      divergences delta.
    - Recommended next phase: ___ (if GO/CONDITIONAL-GO) or "revisit-when"
      condition (if NO-GO).

  (2) Phase 25 results in one table:

  | Sub-phase | Deliverable | Verdict |
  |-----------|-------------|---------|
  | 25-00 | RESEARCH.md (literature synthesis, 9 directions) | DONE |
  | 25-00b | API_BRIDGING_STUDY.md (pyhgf -> reference impl mapping) | (cite) |
  | 25-01 | THEORETICAL_VALIDATION.md (lambda_hat scan, K prediction) | (cite) |
  | 25-02 | ALGORITHM_DECISION.md (PIEKS / quasi-ELK / ...) | (cite) |
  | 25-03 | REPRODUCTION_NOTES.md (reference benchmark match) | (cite) |
  | 25-04 | HGF_PROTOTYPE_NOTES.md (rel-err + GPU timing) | (cite) |
  | 25-05 | RECOVERY_COMPARISON.md (NUTS recovery match) | (cite) |
  | 25-06 | THIS document | GO/NO-GO/CONDITIONAL-GO |

  (3) Speedup analysis at production shape (Scenario A):
    - Cite results/diagnostics/hgf_pscan_proto.json shape (P=300, T=420):
      par_wall = X seconds, seq_wall = Y seconds, speedup = Y/X.
    - Cite Phase 14.2 variants 1-5 sequential-baseline numbers (commits
      0055408..f35698f) for comparison. Phase 14.2 was attacking the
      sample-bound 12-15h window_adaptation; the speedup vs THAT baseline is
      the relevant production comparison.
    - Verdict for Scenario A: typically marginal at T=420 per 25-RESEARCH.md
      §1 and §9 prediction (0.5-2x); cite the specific observed number.

  (4) Speedup analysis at T>>420 (Scenario B):
    - Cite results/diagnostics/hgf_pscan_proto.json shape (P=50, T=2000):
      par_wall, seq_wall, speedup.
    - Extrapolation: at what T does the speedup justify the maintenance
      burden? Cite from 25-RESEARCH.md §9 win-zone analysis.
    - Foreseen future phases that benefit from Scenario B speedup:
      - 4-level HGF (more state = larger D, but T also grows due to
        longer-context modeling).
      - Branched / multimodal HGF (cross-branch coupling).
      - Trial-varying theta (PAT-RL Model D's lambda * delta_HR(t)).
      - Online / streaming inference.
      - Hierarchical pooling at the participant level.
    - Verdict for Scenario B: typically strongly positive (5-20x) per 25-04
      timing.

  (5) Recovery / correctness summary:
    - 25-05 verdict: PASS / FAIL.
    - If FAIL: parallel scan introduces a regression; verdict for THIS plan is
      automatically NO-GO until the regression is fixed.
    - If PASS: statistical equivalence confirmed; correctness is not a blocker.

  (6) Maintenance burden analysis:
    - The chosen reference impl (PIEKS sqrt-parallel-smoothers OR
      lindermanlab/elk) is an external dependency.
    - For productionization: vendor the reference impl into
      `src/prl_hgf/fitting/_pscan/` (NOT pip-installed) so the project
      controls the version.
    - JAX-version compatibility: parallel scan uses `jax.lax.associative_scan`
      and `jax.custom_vjp`; both are stable in JAX core but may break across
      major version bumps.
    - pyhgf upstream changes: HGF update equations could change; the lifted
      `hgf_step` would need to follow.
    - Gradient-through-iteration brittleness: implicit-VJP through PIEKS/ELK
      fixed point requires `jax.custom_root` or `custom_vjp`. Any change to
      the iteration scheme breaks gradients silently.

  (7) User-facing API change (if GO or CONDITIONAL-GO):
    - Production fitting entry points (e.g. `fit_batch_hierarchical(...)` in
      `hierarchical.py`) get a new flag:
      `fit_method='nuts_sequential' | 'nuts_parallel' | 'vb_laplace'`.
      Default remains 'nuts_sequential' (current behavior).
    - The 'nuts_parallel' path imports `parallel_scan_proto` (or its
      productionized equivalent under `src/prl_hgf/fitting/parallel_scan/`).
    - For Scenario B phases: 'nuts_parallel' becomes the default.

  (8) Verdict (one of three; choose based on Section 3-7 synthesis):

  **GO** (likely if Scenario A speedup > 1.5x AND 25-05 PASS):
    - Productionize parallel scan as a fit_method option for both
      pick_best_cue and PAT-RL.
    - Recommended next phase: "Phase 26: Productionize parallel-scan HGF
      likelihood for pick_best_cue and PAT-RL."
    - Promote to v1.4 milestone.

  **CONDITIONAL-GO** (most likely outcome):
    - Productionize parallel scan ONLY for new Scenario B phases (T>>420,
      branched, trial-varying), NOT as drop-in replacement at T=420.
    - Recommended next phase: keep parallel-scan prototype available but do
      not productionize until a Scenario B phase is planned. The Scenario B
      phase plan would include the productionization as a sub-task.
    - Conditions to revisit retroactive productionization: (a) a >2x speedup
      at T=420 is observed on later GPU generations, OR (b) a structural
      improvement to the prototype (e.g. avoiding the warm-up cost) is
      developed.
    - Do NOT promote to v1.4 yet.

  **NO-GO** (likely only if 25-05 FAIL or 25-04 numerical agreement issues):
    - Phase 25 produced a research record but no production change.
    - Document the failure mode for revisit when the field advances (KLA,
      arXiv:2602.10743, is the successor architecture per 25-RESEARCH.md §6
      open question 6).
    - Recommended next phase: none related to parallel scan; revisit when:
      (a) KLA matures into a published-and-benchmarked HGF-applicable form, OR
      (b) HGF moves to a structure (4-level, branched) where the marginal
      analysis flips. Revisit period: 12 months.

  (9) Risk register:

  | Risk | Severity | Mitigation |
  |------|----------|------------|
  | Reference impl (sqrt-parallel-smoothers / elk) becomes unmaintained | MEDIUM | Vendor into project; track upstream periodically |
  | JAX major version bump breaks associative_scan / custom_vjp | MEDIUM | Pin JAX version; test on version bumps |
  | pyhgf upstream changes break the lifted hgf_step | MEDIUM | Pin pyhgf version; re-validate on bumps |
  | Gradient-through-iteration silently produces wrong gradients on edge cases | HIGH | Maintain proto_grad_check.py as a regression test in CI |
  | T=420 GPU speedup degrades on next-gen GPUs (different occupancy regime) | LOW | Periodic re-benchmark |
  | (more from 25-00b risk register, 25-04 issues) | ... | ... |

  (10) Recommended next phase (if GO or CONDITIONAL-GO):
    - Phase number: ___ (next available; current ROADMAP through 24).
    - Phase name: "Productionize parallel-scan HGF likelihood [scope]".
    - Sub-phase outline:
      - 26-00: vendor reference impl into src/prl_hgf/fitting/_pscan/.
      - 26-01: integrate as fit_method='nuts_parallel' in hierarchical.py and
        hierarchical_patrl.py.
      - 26-02: regression-test parameter recovery on production-scale cohort
        (P=300, T=420).
      - 26-03: full production NUTS run on real data; compare results to
        existing 'nuts_sequential' baseline.
      - 26-04: documentation update (CLAUDE.md, README) to surface the new
        fit_method option.

  (11) Cross-references and appendix:
    - All sub-phase summary paths.
    - Key results JSON paths.
    - 25-RESEARCH.md sections cited.

  Keep memo under ~800 lines. This is the longest Phase 25 memo because it's
  the closing synthesis.
  </action>
  <verify>
  1. `ls .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` exits 0.
  2. `grep -cE "^GO|^NO-GO|^CONDITIONAL-GO|verdict.*GO" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 1 (verdict is explicit).
  3. `grep -c "T=420\|T>>420\|T >> 420\|Scenario A\|Scenario B" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 4 (T-distinction is addressed thoroughly).
  4. `grep -c "Phase 14.2\|variant" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 1 (Phase 14.2 baseline cited).
  5. `grep -c "Risk\|risk" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 3 (risk register populated).
  6. `grep -c "Recommended next phase\|next phase\|recommend" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 1.
  7. `grep -c "API\|fit_method\|user-facing" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 2 (API change described).
  8. `grep -c "speedup" .planning/phases/25-parallel-scan-acceleration-research/PRODUCTIONIZATION_VERDICT.md` >= 3.
  </verify>
  <done>
  Memo synthesizes all 7 prior sub-phase deliverables, names a single verdict
  (GO/NO-GO/CONDITIONAL-GO), distinguishes the T=420 vs T>>420 production
  scenarios, names a recommended next phase if applicable, populates the risk
  register.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>
  Confirm or override the productionization verdict and recommended next phase.
  </decision>
  <context>
  This closes Phase 25. The verdict + recommended next phase shapes the
  remaining v1.3 / v1.4 milestone trajectory. A GO commits to a Phase 26
  productionization sprint. A CONDITIONAL-GO defers productionization until a
  Scenario B phase is planned. A NO-GO closes the parallel-scan thread.
  </context>
  <options>
    <option id="confirm-verdict">
      <name>Confirm the verdict written in PRODUCTIONIZATION_VERDICT.md</name>
      <pros>Memo synthesis is data-driven from all 7 sub-phases; user trusts the analysis</pros>
      <cons>Verdict may be more conservative or aggressive than user's product judgment; CONDITIONAL-GO is the most likely default and may be too cautious if the speedup at T=420 was strongly positive</cons>
    </option>
    <option id="override-to-go">
      <name>Override to GO regardless of memo verdict</name>
      <pros>If the user wants to commit to a Phase 26 productionization sprint based on the long-term Scenario B value alone (even if Scenario A is marginal)</pros>
      <cons>Maintenance burden of two NUTS paths is non-trivial; user takes on this debt without Scenario A justification</cons>
    </option>
    <option id="override-to-conditional">
      <name>Override to CONDITIONAL-GO with explicit conditions</name>
      <pros>Preserves Phase 25 prototype as a research artifact without committing to productionization until a concrete Scenario B phase exists; lowest-regret option</pros>
      <cons>Defers committing to v1.4 promotion; the prototype lives in src/prl_hgf/fitting/parallel_scan_proto.py as 'inactive'</cons>
    </option>
    <option id="override-to-no-go">
      <name>Override to NO-GO and close Phase 25 as research-only</name>
      <pros>Cleanest exit if 25-05 had any pathology or if the speedup at all shapes was disappointing</pros>
      <cons>Phase 25 work becomes pure research record; no production benefit captured</cons>
    </option>
  </options>
  <resume-signal>Select: confirm-verdict, override-to-go, override-to-conditional, or override-to-no-go. The orchestrator will record the choice and update ROADMAP.md accordingly.</resume-signal>
</task>

</tasks>

<verification>
1. PRODUCTIONIZATION_VERDICT.md is on disk with all 11 sections.
2. Verdict (GO / NO-GO / CONDITIONAL-GO) is explicit and supported by sub-phase data.
3. Scenario A (T=420) and Scenario B (T>>420) are distinguished with specific speedup numbers.
4. Phase 14.2 baseline is cited.
5. Risk register has >= 5 risks documented.
6. If GO/CONDITIONAL-GO, recommended next phase is named.
7. User has confirmed (or overridden) the verdict.
</verification>

<success_criteria>
- Phase 25 has a single closing verdict with full sub-phase synthesis.
- Recommended next phase (if any) is named for ROADMAP planning.
- T=420 vs T>>420 production scenarios are quantitatively distinguished per
  25-RESEARCH.md §1 / §9 framing.
- Phase 25 deliverables are all on disk and cross-referenced.
</success_criteria>

<output>
After completion, create `.planning/phases/25-parallel-scan-acceleration-research/25-06-SUMMARY.md`
with: the verdict, headline speedup numbers (P=300 T=420 and P=50 T=2000),
recovery RMSE / ESS / divergences match, the recommended next phase, and
pointers to PRODUCTIONIZATION_VERDICT.md and all prior sub-phase summaries.
</output>
</content>

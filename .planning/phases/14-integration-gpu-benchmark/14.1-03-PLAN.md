---
phase: 14-integration-gpu-benchmark
plan: "14.1-03"
type: execute
wave: 2
depends_on: ["14.1-01"]
supersedes: ["14.1-02"]
files_modified:
  - results/power/benchmark_batched.json
  - .planning/STATE.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "cluster/14_benchmark_gpu.slurm is submitted on M3 with SAMPLER=blackjax (the 14.1-01 default; no --export needed)"
    - "The submission writes a BlackJAX-sampler Phase 14 benchmark JSON to results/power/benchmark_batched.json on the cluster, and the script's git auto-push block (added in 14.1-01) pushes it plus logs plus updated STATE.md to origin/main automatically"
    - "On the local machine, git pull lands the BlackJAX benchmark_batched.json and the cluster-written Benchmark decision gate row in STATE.md; both contain all Phase 14 schema fields and finite values"
    - "No numpyro archive is produced: the prior in-flight numpyro run was erroneous and left no artifact to preserve (superseded plan 14.1-02)"
  artifacts:
    - path: "results/power/benchmark_batched.json"
      provides: "BlackJAX Phase 14 benchmark output (production sampler)"
      contains: "per_iteration_s"
    - path: ".planning/STATE.md"
      provides: "Benchmark decision gate row (sampler=blackjax)"
      contains: "Benchmark decision gate"
  key_links:
    - from: "cluster/14_benchmark_gpu.slurm"
      to: "results/power/benchmark_batched.json"
      via: "git auto-push block pushes benchmark result to origin/main"
      pattern: "git push origin main"
    - from: "results/power/benchmark_batched.json"
      to: ".planning/STATE.md"
      via: "_update_state_md appends Benchmark decision gate row from benchmark JSON"
      pattern: "Benchmark decision gate"
---

<objective>
Submit cluster/14_benchmark_gpu.slurm on M3 with the BlackJAX sampler so Phase 15's production partition/walltime decision is made against the same sampler Phase 15 will use. STATE.md decision 98 (BlackJAX is the default production sampler) and decision 105 (JIT gate thresholds are numpyro-conservative; will tighten post-cluster-bench) both require a BlackJAX bench pass.

This is the sole remaining benchmark-execution gap: the earlier in-flight numpyro run was launched in error and produced no artifact worth preserving, so plan 14.1-02 has been superseded and this plan stands alone.

This plan depends only on 14.1-01 (SLURM patch: SAMPLER env-var default=blackjax + git auto-push block).

Purpose: Close VERIFICATION.md SC3 (benchmark_batched.json missing) and SC4 (STATE.md decision-gate row missing) with production-sampler numbers so Phase 15 can commit to a partition without re-benchmarking.

Output: results/power/benchmark_batched.json written by BlackJAX bench run, auto-pushed from the cluster; STATE.md gains a Benchmark decision gate row; both land on local after `git pull`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
@.planning/phases/14-integration-gpu-benchmark/14.1-01-PLAN.md
@cluster/14_benchmark_gpu.slurm
@scripts/08_run_power_iteration.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Submit BlackJAX benchmark on M3</name>
  <what-built>
  cluster/14_benchmark_gpu.slurm patched in 14.1-01 with SAMPLER env-var (default blackjax) and git auto-push block. The patch is on origin/main.
  </what-built>
  <how-to-verify>
  On M3:
    1. `cd ~/psilocybin_prl_analyses` (or wherever the repo lives on M3).
    2. `git pull origin main` — confirms 14.1-01 patch is present.
    3. Verify patch landed:
       - `grep -n 'SAMPLER' cluster/14_benchmark_gpu.slurm` shows `export SAMPLER="${SAMPLER:-blackjax}"` and `--sampler "${SAMPLER}"`.
       - `grep -n 'git push origin main' cluster/14_benchmark_gpu.slurm` shows the auto-push block.
    4. Confirm no stale benchmark JSON will be overwritten unexpectedly:
       - `ls results/power/benchmark_batched.json 2>&1` — either "No such file or directory" (expected) or an old file (safe to overwrite since we have no numpyro baseline to preserve).
    5. Submit: `sbatch cluster/14_benchmark_gpu.slurm`
       (no --export needed; blackjax is the default after 14.1-01).
    6. Record the returned JOB_ID.
    7. Monitor: `squeue -u $USER -n prl_bench14` shows it PD then R; `tail -f cluster/logs/bench14_<JOB_ID>.out` once it starts running.
    8. When it finishes: the git auto-push block in the SLURM script (added in 14.1-01) commits + pushes `cluster/logs/bench14_<JOB_ID>.{out,err}`, `results/power/benchmark_batched.json`, and `.planning/STATE.md` to origin/main. Confirm the push arrived on the cluster side: `git log origin/main --oneline -3`.

  On local machine after cluster push:
    9. `git pull origin main` — pulls the BlackJAX benchmark JSON + STATE.md update + logs.
    10. `python -m json.tool results/power/benchmark_batched.json` shows valid JSON. The `mcmc_settings.sampler` field may still hard-code "numpyro-direct" in scripts/08_run_power_iteration.py line ~1087 (minor schema drift; surface in SUMMARY but not block on). The load-bearing field is `per_iteration_s`.

  Expected outcome: results/power/benchmark_batched.json is a fresh BlackJAX run with finite per_iteration_s / gpu_hours_per_chunk / decision; STATE.md has a new Benchmark decision gate row.
  </how-to-verify>
  <resume-signal>
  Paste one of:
    - "blackjax benchmark completed, job <JOB_ID>, decision=<gpu|cpu_comp>, gpu_hours_per_chunk=<value>, per_iteration_s=<value>"
    - "blackjax benchmark failed, err log: <paste last 40 lines>"
    - "blackjax benchmark timed out at 8h — numbers to date: <paste>"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify schema, disambiguate STATE.md row, record outcome in SUMMARY</name>
  <files>.planning/STATE.md</files>
  <action>
  Assumes Task 1 resumed with success and `git pull origin main` landed the BlackJAX benchmark JSON + STATE.md update.

  Step 1 — Schema verification:
  Read results/power/benchmark_batched.json. Confirm all required Phase 14 schema fields are present and finite:
    - per_iteration_s (float > 0)
    - peak_vram_mb (float >= 0; may be 0 if nvidia-smi sampling failed)
    - mean_gpu_util_pct (float 0-100)
    - decision ("gpu" or "cpu_comp")
    - gpu_hours_per_chunk (float > 0)
    - jit_cold_s (float > 0)
    - jit_warm_s (float >= 0)
    - benchmark_n_participant_sessions (int)
    - grid (dict with n_per_group, max_n, effect_sizes, n_iterations, n_chunks)
    - mcmc_settings (dict with chains, draws, tune, sampler)
  If any required field is missing or non-finite, surface a clear error to the user and STOP — do not fabricate values.

  Step 2 — STATE.md row disambiguation:
  The cluster-side `_update_state_md` appended a row labelled "Benchmark decision gate" (bare; no sampler tag — scripts/08_run_power_iteration.py line ~274). Relabel it so future re-benchmarks (e.g. if a Phase 20 task adds a trial-varying omega path with different JIT cost) can coexist as sibling rows without collision. Edit the first column from:
    `Benchmark decision gate: <decision> (<hours> GPU-hrs/chunk)`
  to:
    `Benchmark decision gate sampler=blackjax: <decision> (<hours> GPU-hrs/chunk)`
  Change the phase tag column from `14-02` to `14.1-03`.

  Step 3 — Commit:
    git add .planning/STATE.md
    git commit -m "docs(14.1-03): label blackjax benchmark row"
    git push origin main

  Do NOT revise the JIT gate thresholds in `apply_decision_gate` in this plan — STATE.md decision 105 says thresholds will tighten post-cluster-bench, but that tightening belongs in a future plan (Phase 15 planning or a quick-NNN), not gap closure. Surface a recommendation in the SUMMARY about whether the BlackJAX numbers warrant threshold tightening, but leave the code alone.
  </action>
  <verify>
  1. `grep -c "Benchmark decision gate" .planning/STATE.md` returns >= 1.
  2. `grep "sampler=blackjax" .planning/STATE.md` returns the relabelled row.
  3. `grep -cE "Benchmark decision gate:( |$)" .planning/STATE.md` returns 0 — no bare/untagged rows remain.
  4. `ls results/power/benchmark_batched.json` exits 0.
  5. `git log --oneline -1 -- .planning/STATE.md` shows the `docs(14.1-03)` label commit.
  </verify>
  <done>
  STATE.md has a disambiguated Benchmark decision gate row (sampler=blackjax); benchmark JSON on disk with valid schema; cluster and local filesystems in sync via auto-push + git pull.
  </done>
</task>

</tasks>

<verification>
1. Single labelled Benchmark decision gate row in STATE.md (sampler=blackjax).
2. results/power/benchmark_batched.json contains BlackJAX run with all Phase 14 schema fields finite.
3. SUMMARY records per_iteration_s, gpu_hours_per_chunk, and decision so Phase 15 planning can read the verdict without re-running anything.
4. JIT gate thresholds in iteration.py `apply_decision_gate` are UNCHANGED (deferred to future planning).
</verification>

<success_criteria>
- Phase 15 readiness: authoritative per_iteration_s + gpu_hours_per_chunk for the production sampler (BlackJAX) exist on disk.
- STATE.md reflects the bench pass with a sampler-tagged label so future re-benchmarks coexist as siblings.
- Cluster filesystem and local filesystem are in sync via git auto-push block from 14.1-01.
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-03-SUMMARY.md` with the BlackJAX benchmark numbers: per_iteration_s, peak_vram_mb, mean_gpu_util_pct, decision, gpu_hours_per_chunk, jit_cold_s, jit_warm_s. Include a recommendation: "Phase 15 should use <partition> based on BlackJAX decision=<value> and gpu_hours_per_chunk=<value>." If the BlackJAX run strongly undershoots the 50 GPU-hour threshold, note that the JIT gate thresholds in `iteration.py::apply_decision_gate` are now demonstrably conservative and can be tightened in a future plan.
</output>

---
phase: 14-integration-gpu-benchmark
plan: "14.1-07c"
type: execute
wave: 3
depends_on: ["14.1-07b"]
files_modified:
  - .planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "14.1-07-SUMMARY.md exists and names the active infrastructure strategy (shared-dir or nodelist-fallback) and cites the VERIFICATION.md quantitative numbers verbatim"
    - "14.1-07-SUMMARY.md contains a single unambiguous handoff command that Plan 14.1-03 runs to execute the BlackJAX production benchmark (e.g. `sbatch cluster/14_benchmark_gpu.slurm` or the pinned variant) — this is the phase-close unblock signal"
    - "STATE.md is updated: any decisions row that cited the old per-node cache behaviour is amended with a pointer to the 14.1-07 fix (additive note, not delete); pending-todos/blockers no longer list `cache_key cross-process miss` as blocking 14.1-03"
    - "ROADMAP.md 14.1 chain: the 14.1-07-PLAN.md checkbox is marked `[x]` with a short gloss referencing the active strategy; the 14.1-03-PLAN.md line is annotated to note that 14.1-07's infrastructure prerequisite is satisfied"
    - "No further infrastructure code changes land in this plan — it is pure documentation / state-bookkeeping at phase close"
  artifacts:
    - path: ".planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md"
      provides: "Phase-close summary naming active strategy, quantitative evidence, and 14.1-03 handoff command"
      contains: "14.1-03"
    - path: ".planning/STATE.md"
      provides: "Updated project state with Phase 14.1-07 close row"
      contains: "14.1-07"
    - path: ".planning/ROADMAP.md"
      provides: "14.1 chain reflects 14.1-07 closure and 14.1-03 unblock"
      contains: "14.1-07"
  key_links:
    - from: ".planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md"
      to: ".planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md"
      via: "SUMMARY cites quantitative gate numbers from VERIFICATION"
      pattern: "next_proc_warm_s"
    - from: ".planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md"
      to: ".planning/phases/14-integration-gpu-benchmark/14.1-03-PLAN.md"
      via: "SUMMARY names the exact sbatch command Plan 14.1-03 executes"
      pattern: "sbatch cluster/14_benchmark_gpu.slurm"
    - from: ".planning/ROADMAP.md 14.1 chain"
      to: "14.1-07-PLAN.md, 14.1-03-PLAN.md"
      via: "roadmap checkboxes and annotations"
      pattern: "14.1-07"
---

<objective>
Close Phase 14.1-07 with a single consolidated SUMMARY that (a) restates the active infrastructure strategy proven in VERIFICATION.md, (b) unblocks Phase 14.1-03 with the exact sbatch command to run, and (c) propagates the close-out into STATE.md and ROADMAP.md.

This plan is autonomous and handoff-focused. It does NOT submit the production benchmark (`sbatch cluster/14_benchmark_gpu.slurm`) — that is Plan 14.1-03's deliverable. Per CONTEXT ("Plan may split into two commits: patch + Step-1 pass → 14.1-07 SUMMARY; Step-2 success → 14.1-03 SUMMARY"), and per gsd-planner directive #1 (recommended handoff-to-14.1-03 option), 14.1-07 closes at Step-1 verification pass and transfers Step-2 execution responsibility to 14.1-03.

Purpose: The executor for 14.1-03 needs a single, unambiguous "here is the command to run" sentence. Without this hand-off, 14.1-03 would have to re-derive the sbatch command from VERIFICATION.md + VERDICT.md, which is slower and error-prone (especially in the nodelist-fallback branch where the sbatch command is identical but the underlying SLURM script now carries a `#SBATCH --nodelist=` directive).

Output: 14.1-07-SUMMARY.md naming the active strategy + handoff command; updated STATE.md + ROADMAP.md reflecting phase close.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
@.planning/phases/14-integration-gpu-benchmark/14.1-07a-SUMMARY.md
@.planning/phases/14-integration-gpu-benchmark/14.1-07b-SUMMARY.md
@.planning/phases/14-integration-gpu-benchmark/14.1-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write consolidated 14.1-07-SUMMARY.md</name>
  <files>.planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md</files>
  <action>
  Step 1 — Read the three inputs:
    - .planning/phases/14-integration-gpu-benchmark/14.1-07a-SUMMARY.md (Plan A patch summary: commit SHA + files touched + lineage notes)
    - .planning/phases/14-integration-gpu-benchmark/14.1-07b-SUMMARY.md (Plan B verification summary: active strategy + gate numbers)
    - .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md (authoritative evidence)

  Step 2 — Write the consolidated SUMMARY with this structure:

    ---
    phase: 14-integration-gpu-benchmark
    plan: "14.1-07"
    status: complete
    dated: <today's ISO date>
    active_strategy: <shared-dir | nodelist-fallback>   # from VERIFICATION
    unblocks: ["14.1-03"]
    consumes: ["21-07 VERDICT"]
    ---

    # Phase 14.1-07 — Node-Pin Gap Closure (SUMMARY)

    ## Outcome

    <one paragraph: Phase 21 VERDICT §(c) applied; repo-wide cache-dir change committed in 14.1-07a; diagnostic P-sweep in 14.1-07b confirmed quantitative invariants; active strategy is <shared-dir | nodelist-fallback>>

    ## Active Infrastructure Strategy

    <one paragraph describing the active strategy and why it was chosen (if nodelist-fallback, cite the specific gate that failed for shared-dir and the observed numbers)>

    ## Quantitative Evidence (verbatim from 14.1-07-VERIFICATION.md)

    | Gate | Target | Observed |
    |------|--------|----------|
    | Primary: cold/next_proc_warm @ P=50 | ≥ 3.0× | <value>× |
    | Secondary: cold_jit_s @ P=50 | ≤ 300s | <value>s |
    | Regression: same_proc_warm/cold @ P=5 | ≥ 2.0× | <value>× |

    ## Files Modified

    - Plan 14.1-07a (commit <SHA>): 9 SLURM scripts + scripts/08_run_power_iteration.py _run_benchmark Phase-1 upgrade
    - (if fallback) Plan 14.1-07b (commit <SHA>): cluster/14_benchmark_gpu.slurm #SBATCH --nodelist= directive

    ## Handoff to 14.1-03

    Phase 14.1-03 can now execute its production benchmark. The single command is:

        sbatch cluster/14_benchmark_gpu.slurm

    <one sentence: if active_strategy=shared-dir, the command is unchanged from 14.1-03's original scope. If active_strategy=nodelist-fallback, the command is identical — the node pinning is inside the SLURM file itself — but 14.1-03's Task-1 sanity check `grep -c '#SBATCH --nodelist' cluster/14_benchmark_gpu.slurm` should return 1, not 0>

    Expected outcome per VERDICT §e: cold JIT < 500s, warm JIT / cold JIT > 3×, per_iteration_s finite in results/power/benchmark_batched.json.

    ## Explicitly Not Done (boundary to 14.1-03)

    - Did NOT submit sbatch cluster/14_benchmark_gpu.slurm — 14.1-03's deliverable.
    - Did NOT write results/power/benchmark_batched.json — 14.1-03's deliverable.
    - Did NOT update STATE.md Benchmark decision gate row — 14.1-03 populates via _update_state_md.
    - Did NOT tighten apply_decision_gate thresholds (STATE #105 deferred).

    ## Phase 21 Consumer Status

    Phase 21 VERDICT §(c) PR-level fix recommendation: IMPLEMENTED.
    VERDICT §(e) quantitative invariants: VERIFIED at P=50 per VERIFICATION.md.
    VERDICT §(f) 14.1-03 unblock signal: ISSUED by this SUMMARY.

    ## Residual Concerns Referred Forward

    - P=20 in-process warm pathology (1.09× speedup vs 2.0× at P=5/50) — VERDICT §(g) residual backlog note. Not blocking 14.1-03.
    - apply_decision_gate threshold tightening — STATE #105, post-14.1-03.
    - JAX #22281 tracing paradox — only relevant if nodelist-fallback fires AND fails.

    ## Provenance

    - Plan 14.1-07a: .planning/phases/14-integration-gpu-benchmark/14.1-07a-SUMMARY.md
    - Plan 14.1-07b: .planning/phases/14-integration-gpu-benchmark/14.1-07b-SUMMARY.md
    - Step-1 VERIFICATION: .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
    - Source VERDICT: .planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md
  </action>
  <verify>
  1. `.planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md` exists.
  2. `grep '^status:' .planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md` shows `complete`.
  3. SUMMARY cites the exact sbatch command for 14.1-03 and the quantitative gate numbers.
  4. SUMMARY links both 14.1-07a-SUMMARY.md and 14.1-07b-SUMMARY.md plus 14.1-07-VERIFICATION.md.
  </verify>
  <done>
  14.1-07-SUMMARY.md is the single authoritative close-out doc for the plan; 14.1-03 can execute from this doc alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md; commit and push phase-close docs</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md
  </files>
  <action>
  Step 1 — STATE.md: Append (do NOT delete prior rows — STATE is accumulative) a new decision or phase-close row under the "Current Focus" section (or the appropriate STATE.md section per local convention — read the file first to check the header naming). Content:

    - Phase 14.1-07 complete (YYYY-MM-DD). Active strategy: <shared-dir | nodelist-fallback>. Cluster JAX persistent-compilation-cache now shared via /scratch/.../.jax_cache_{gpu|cpu}[/subdir]/shared across all 9 GPU/CPU SLURM scripts. Next_proc_warm/cold ratio at P=50 = <value>× (target ≥ 3.0×). Phase 14.1-03 unblocked; see 14.1-07-SUMMARY.md for handoff command.

  Also amend (if present) any prior row that cited the old per-node cache behaviour with a trailing note:
    "(amended 2026-04-19 — see 14.1-07-SUMMARY.md)"
  Do NOT delete the original text; additive-only change per project convention.

  Pending-todos / blockers: if STATE.md has a todo or blocker naming `cache_key cross-process miss` or `benchmark_batched.json missing` as blocking 14.1-03, strike it through with a trailing "(closed by 14.1-07)" note.

  Step 2 — ROADMAP.md: Update the 14.1 chain listing (lines ~167–172 in the current file; verify by reading first):
    - Mark `- [ ] 14.1-07-PLAN.md — ...` → `- [x] 14.1-07-PLAN.md — Node-pin gap closure (active_strategy=<shared-dir|nodelist-fallback>, next_proc/cold @ P=50 = <value>×, completed YYYY-MM-DD)`.
      (If the roadmap currently has separate rows for 14.1-07a / 14.1-07b / 14.1-07c, check the precedent — if 14.1-06 and prior decimal plans got one row each, preserve that; if they got one consolidated row, use one row.)
    - On the `- [ ] 14.1-03-PLAN.md — ...` line, append: "(prerequisite 14.1-07 satisfied 2026-04-19)".

  Step 3 — Commit the docs atomically:

    git add .planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md \
            .planning/STATE.md \
            .planning/ROADMAP.md

    git commit -m "$(cat <<'EOF'
docs(14.1-07): close phase — active_strategy=<shared-dir|nodelist-fallback>

Phase 14.1-07 complete: cluster infrastructure patch (14.1-07a) + diagnostic
verification (14.1-07b) landed; P=50 next_proc_warm/cold ratio = <value>× vs
target ≥ 3.0×. STATE.md + ROADMAP.md reflect phase close. Phase 14.1-03
unblocked — see .planning/phases/14-integration-gpu-benchmark/14.1-07-SUMMARY.md
for the handoff command.
EOF
)"

    git push origin main

  No Co-Authored-By.
  </action>
  <verify>
  1. `grep -n '14.1-07' .planning/STATE.md` shows at least one new line naming the plan close.
  2. `grep -n '14.1-07' .planning/ROADMAP.md` shows the updated 14.1 chain row(s).
  3. `grep -n '14.1-07-PLAN.md' .planning/ROADMAP.md | grep -E '^[^:]+:- \[x\]'` returns at least one match (the checkbox is marked done).
  4. `grep -n '14.1-03-PLAN.md' .planning/ROADMAP.md` shows the annotation that 14.1-07 is satisfied.
  5. `git log --oneline -1` shows `docs(14.1-07): close phase ...`.
  6. `git rev-parse origin/main == git rev-parse HEAD`.
  </verify>
  <done>
  STATE.md + ROADMAP.md reflect the phase close; docs commit pushed; plan 14.1-07 is a fully resolved chain (a → b → c) on origin/main.
  </done>
</task>

</tasks>

<out_of_scope>
- Do NOT submit `sbatch cluster/14_benchmark_gpu.slurm` — this plan hands off to 14.1-03, it does not run the production benchmark itself.
- Do NOT modify any `src/` or `cluster/` code — this plan is pure documentation.
- Do NOT touch `results/power/benchmark_batched.json` — 14.1-03 owns it.
- Do NOT edit `.planning/STATE.md` decisions 98, 105, 106, 107 — they are referenced but not revised here.
- Do NOT modify VERDICT.md, VERIFICATION.md, 14.1-07a-SUMMARY.md, or 14.1-07b-SUMMARY.md — they are sources-of-truth for this plan's reads.
</out_of_scope>

<verification>
1. 14.1-07-SUMMARY.md unambiguously names the handoff sbatch command for 14.1-03.
2. STATE.md and ROADMAP.md reflect phase close; prior rows preserved (additive only).
3. All commits pushed to origin/main.
4. No Co-Authored-By lines in commit messages.
</verification>

<success_criteria>
- Phase 14.1-07 is a fully resolved plan chain on origin/main (14.1-07a patch + 14.1-07b verification + 14.1-07c hand-off summary + state/roadmap updates).
- Plan 14.1-03's executor can read 14.1-07-SUMMARY.md alone and know exactly which sbatch command to run with zero ambiguity about the underlying strategy.
- Plan 14.1-03 (per its existing PLAN.md) now matches reality — the "grep -c '#SBATCH --nodelist=' cluster/14_benchmark_gpu.slurm should be 1" sanity check mentioned in VERDICT §(f) either returns 0 (shared-dir path; 14.1-03's sanity-check comment should be updated to reflect the shared-dir default — separate follow-up in 14.1-03's own plan revision if needed) or 1 (nodelist-fallback path; 14.1-03's sanity check already matches).
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-07c-SUMMARY.md` with:
- Confirmation that 14.1-07-SUMMARY.md consolidated both sub-plans (a + b) correctly
- STATE.md / ROADMAP.md diff summary (one-line per row changed)
- Single command Plan 14.1-03 runs next (echoed verbatim)
- Phase 14.1-07 final close verdict: complete / complete_with_notes
</output>

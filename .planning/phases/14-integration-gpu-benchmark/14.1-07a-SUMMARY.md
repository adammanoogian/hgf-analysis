---
phase: 14-integration-gpu-benchmark
plan: "14.1-07a"
subsystem: infra
tags: [slurm, jax, xla, cache, cluster, blackjax, benchmark]

# Dependency graph
requires:
  - phase: 21-benchmark-bottleneck-diagnosis
    provides: VERDICT.md naming cache_key (per-node cache scope vs SLURM scheduler mismatch) as dominant bottleneck
provides:
  - Nine SLURM scripts repointed from /${SLURMD_NODENAME:-local} to /shared cache dir
  - scripts/08_run_power_iteration.py Phase-1 warm-up upgraded from N=2 to N=max(n_per_group_grid)
  - Atomic fix commit 6b7e6ed on origin/main ready for M3 git pull
affects: [14.1-07b, 14.1-07c, 14.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared Lustre JAX cache: all SLURM scripts write to .jax_cache_{gpu|cpu}[/subdir]/shared — no per-node scoping"
    - "Production-shape Phase-1 warm-up: warm-up cohort matches Phase-2 cohort size so in-process tracing cache hits"

key-files:
  created: []
  modified:
    - cluster/14_benchmark_gpu.slurm
    - cluster/08_power_sweep.slurm
    - cluster/16_smoke_test_cpu.slurm
    - cluster/16_smoke_test_gpu.slurm
    - cluster/18_smoke_patrl_cpu.slurm
    - cluster/21_diagnose_laplace.slurm
    - cluster/21_diagnose_nuts.slurm
    - cluster/21_diagnose_pscan.slurm
    - cluster/patrl_smoke.slurm
    - scripts/08_run_power_iteration.py

key-decisions:
  - "Drop ${SLURMD_NODENAME:-local} suffix from JAX_COMPILATION_CACHE_DIR in all nine cache-dir SLURM scripts; use /shared subdir for Lustre cross-node reuse"
  - "Phase-1 warm-up cohort upgraded from N=2/group to N=max(n_per_group_grid) so in-process tracing cache hits Phase-2 shape (Jaccard 0.180 between N=2 and N=50 HLO per 21-06 §c)"
  - "Eight banner-only SLURM scripts left untouched — SLURMD_NODENAME in echo/Node lines is operator visibility, not cache-dir routing"
  - "Single atomic commit so git revert <hash> rolls back the entire repo-wide infrastructure change"

patterns-established:
  - "All cluster SLURM scripts in this repo write JAX_COMPILATION_CACHE_DIR to a shared subdir, never per-node"

# Metrics
duration: 20min
completed: 2026-04-19
---

# Phase 14.1 Plan 07a: Node-Pin Infrastructure Patch Summary

**Nine SLURM scripts patched from per-node JAX cache dirs to shared Lustre path; Phase-1 warm-up upgraded from N=2 to production N=max(n_per_group_grid) — implements Phase 21 VERDICT §c fix as single atomic commit 6b7e6ed on origin/main.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-04-19T18:00:00Z
- **Completed:** 2026-04-19T18:03:08Z
- **Tasks:** 3 (Task 1: lineage probe; Task 2: patch edits; Task 3: atomic commit + push)
- **Files modified:** 10

## Accomplishments

- Dropped `${SLURMD_NODENAME:-local}` suffix from `JAX_COMPILATION_CACHE_DIR` in all nine cache-dir SLURM scripts; cache now routes to `/shared` via Lustre mount, enabling cross-node cross-job cache reuse without scheduler node affinity
- Upgraded `_run_benchmark` Phase-1 warm-up from N=2/group to `N=max(power_config.n_per_group_grid)` with production `args.fit_chains/fit_draws/fit_tune` so the in-process JAX tracing cache actually hits for Phase-2 (eliminates the Jaccard-0.180 shape mismatch between N=2 and N=50 HLO scan bodies)
- Eight banner-only SLURM scripts intentionally left untouched (operator visibility preserved)
- Single atomic commit pushed to origin/main: M3 can `git pull` and run `bash cluster/21_submit_pscan_chain.sh` immediately for Plan 14.1-07b verification

## Patched SLURM Scripts (9 of 17)

| Script | Cache path before | Cache path after |
|--------|-------------------|-----------------|
| `cluster/14_benchmark_gpu.slurm` | `.jax_cache_gpu/${SLURMD_NODENAME:-local}` | `.jax_cache_gpu/shared` |
| `cluster/08_power_sweep.slurm` | `.jax_cache_gpu/${SLURMD_NODENAME:-local}` | `.jax_cache_gpu/shared` |
| `cluster/16_smoke_test_cpu.slurm` | `.jax_cache_cpu/${SLURMD_NODENAME:-local}` | `.jax_cache_cpu/shared` |
| `cluster/16_smoke_test_gpu.slurm` | `.jax_cache_gpu/${SLURMD_NODENAME:-local}` | `.jax_cache_gpu/shared` |
| `cluster/18_smoke_patrl_cpu.slurm` | `.jax_cache_cpu/patrl/${SLURMD_NODENAME:-local}` | `.jax_cache_cpu/patrl/shared` |
| `cluster/21_diagnose_laplace.slurm` | `.jax_cache_gpu/${SLURMD_NODENAME:-local}` | `.jax_cache_gpu/shared` |
| `cluster/21_diagnose_nuts.slurm` | `.jax_cache_gpu/${SLURMD_NODENAME:-local}` | `.jax_cache_gpu/shared` |
| `cluster/21_diagnose_pscan.slurm` | `.jax_cache_gpu/${SLURMD_NODENAME:-local}` | `.jax_cache_gpu/shared` |
| `cluster/patrl_smoke.slurm` | `.jax_cache_cpu/patrl/${SLURMD_NODENAME:-local}` | `.jax_cache_cpu/patrl/shared` |

## Why the 8 Banner-Only Scripts Were NOT Patched

The following scripts reference `${SLURMD_NODENAME}` ONLY in `echo "Node: ${SLURMD_NODENAME:-$(hostname)}"` banner lines for operator visibility in SLURM logs — not in `JAX_COMPILATION_CACHE_DIR` export lines:

- `cluster/01_diagnostic_gpu.slurm`
- `cluster/04_fit_mcmc_gpu.slurm`
- `cluster/05_validation_gpu.slurm`
- `cluster/06_analysis.slurm`
- `cluster/09_power_postprocess.slurm`
- `cluster/14_valid03_cpu.slurm`
- `cluster/14_valid03_gpu.slurm`
- `cluster/99_push_results.slurm`

These banner lines display which node SLURM allocated for fallback debugging purposes and must be preserved unchanged per plan scope. They do not route any cache writes through per-node paths.

## Commit SHA + Push Status

- **Atomic fix commit:** `6b7e6ed` (`fix(14.1-07): drop per-node cache suffix + upgrade Phase-1 warm-up to production N`)
- **Push status:** SUCCEEDED — `git rev-parse origin/main == git rev-parse HEAD == 6b7e6ed2521b3b106952b7812c73de3b73d18088`
- **Files in commit:** 10 (9 SLURM + 1 Python), 48 insertions / 30 deletions

## Task 1 Lineage Notes

**No prior lineage found** — this is the first cluster cache-scope patch in `.planning/` artifact history.

The 14.1-07-CONTEXT.md line 34 documents a user verbal caution: "User recalls having tried a single-subprocess restructure before and hitting 'similar problems'." However, no prior SUMMARY file, STATE.md row, or commit in `.planning/` documents a concrete applied cache-dir path restructure. The closest prior artifact is `quick-003` (Decision 106) which restructured `_build_sample_loop` to pass data as traced JIT args — a Python-level change to `hierarchical.py`, not a SLURM cache-dir path change.

The single-subprocess concern from CONTEXT is about a different mechanism (wrapping the benchmark in a subprocess to reuse the cache across artificial process boundaries) — that approach is not attempted here. This plan exclusively patches the `JAX_COMPILATION_CACHE_DIR` path suffix, which is orthogonal to subprocess restructuring.

## Task Commits

This plan issues a single atomic commit per the CONTEXT.md rollback-unit directive:

1. **Task 1** (lineage probe) — read-only, no commit
2. **Task 2** (patch edits) — no commit (staged only, per plan directive)
3. **Task 3** (atomic commit + push) — `6b7e6ed` `fix(14.1-07): drop per-node cache suffix + upgrade Phase-1 warm-up to production N`

## Files Created/Modified

- `cluster/14_benchmark_gpu.slurm` — Cache dir comment updated + SLURMD_NODENAME suffix dropped
- `cluster/08_power_sweep.slurm` — Cache dir comment updated + SLURMD_NODENAME suffix dropped
- `cluster/16_smoke_test_cpu.slurm` — SLURMD_NODENAME suffix dropped
- `cluster/16_smoke_test_gpu.slurm` — Cache dir comment updated + SLURMD_NODENAME suffix dropped
- `cluster/18_smoke_patrl_cpu.slurm` — SLURMD_NODENAME suffix dropped
- `cluster/21_diagnose_laplace.slurm` — Comment updated + SLURMD_NODENAME suffix dropped
- `cluster/21_diagnose_nuts.slurm` — Header comment + cache comment updated + SLURMD_NODENAME suffix dropped
- `cluster/21_diagnose_pscan.slurm` — Header comment + cache comment updated + SLURMD_NODENAME suffix dropped
- `cluster/patrl_smoke.slurm` — SLURMD_NODENAME suffix dropped
- `scripts/08_run_power_iteration.py` — Phase-1 block: N=2 → N=max(n_per_group_grid); cfg_tiny/sim_tiny → cfg_warm/sim_warm; n_chains/draws/tune wired to args; VERDICT comment block added

## Decisions Made

- Chosen subdir name: `shared` (explicit, self-documenting, distinct from any existing per-node subdir name so a patched run is immediately identifiable in cache listings)
- `21_diagnose_nuts.slurm` and `21_diagnose_pscan.slurm` header comments updated to remove stale mention of the old `/${SLURMD_NODENAME:-local}` path formula so comments do not lie after the patch
- Python local vars renamed `cfg_tiny`/`sim_tiny` → `cfg_warm`/`sim_warm` to reflect that the cohort is no longer "tiny"
- No `src/prl_hgf/` production code was touched (`git diff --stat src/` for this commit: 0 lines)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. All nine SLURM files had the cache-dir export line in the exact positions specified in the plan. Python AST parse succeeded immediately. Push was fast-forwardable on first attempt.

## Next Phase Readiness

**HANDOFF TO 14.1-07b:**

M3 is ready for:
```
git pull origin main
bash cluster/21_submit_pscan_chain.sh
```

The patched `cluster/21_diagnose_pscan.slurm` will now route all cache writes to `.jax_cache_gpu/shared`. Cold and warm jobs in the `afterok`-chained P-sweep will share the same on-disk cache regardless of which GPU node SLURM assigns. Success criterion: at P=50, `next_proc_warm_s / cold_jit_s >= 3.0x` (baseline was 1.00x).

If the verification gate fails (ratio < 2.0x or corrupt cache entries), Plan 14.1-07b should trigger the `--nodelist=<pinned>` fallback per CONTEXT.md §Decisions and document in VERIFICATION.md without silently reverting.

Plan 14.1-07c (phase-close hand-off) depends on 14.1-07b passing the gate.

---
*Phase: 14-integration-gpu-benchmark*
*Completed: 2026-04-19*

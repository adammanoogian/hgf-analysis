---
phase: 14-integration-gpu-benchmark
plan: "14.1-07a"
type: execute
wave: 1
depends_on: []
files_modified:
  - cluster/14_benchmark_gpu.slurm
  - cluster/08_power_sweep.slurm
  - cluster/16_smoke_test_cpu.slurm
  - cluster/16_smoke_test_gpu.slurm
  - cluster/18_smoke_patrl_cpu.slurm
  - cluster/21_diagnose_laplace.slurm
  - cluster/21_diagnose_nuts.slurm
  - cluster/21_diagnose_pscan.slurm
  - cluster/patrl_smoke.slurm
  - scripts/08_run_power_iteration.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Zero SLURM scripts under cluster/ still use ${SLURMD_NODENAME} in the JAX_COMPILATION_CACHE_DIR path (grep -n 'JAX_COMPILATION_CACHE_DIR=.*SLURMD_NODENAME' cluster/*.slurm returns nothing)"
    - "Nine SLURM scripts (14_benchmark_gpu, 08_power_sweep, 16_smoke_test_cpu, 16_smoke_test_gpu, 18_smoke_patrl_cpu, 21_diagnose_laplace, 21_diagnose_nuts, 21_diagnose_pscan, patrl_smoke) now export a node-agnostic JAX_COMPILATION_CACHE_DIR ending in /shared (or equivalent shared subdir) instead of /${SLURMD_NODENAME:-local}"
    - "Echo/banner lines that display ${SLURMD_NODENAME:-$(hostname)} as the Node diagnostic are preserved (operator visibility for fallback debugging); they are NOT the cache-dir"
    - "scripts/08_run_power_iteration.py _run_benchmark Phase 1 no longer builds a N=2/group cohort; it builds a production-shape cohort at max(n_per_group_grid) so the in-process cold→warm pair exercises the same HLO shape the Phase 2 full-iteration measurement will use"
    - "The atomic commit's message follows fix(14.1-07): prefix and the commit is pushed to origin/main so M3 git pull picks it up for the Plan B verification chain"
  artifacts:
    - path: "cluster/14_benchmark_gpu.slurm"
      provides: "Primary benchmark SLURM with node-agnostic cache dir"
      contains: ".jax_cache_gpu/shared"
    - path: "cluster/21_diagnose_pscan.slurm"
      provides: "P-sweep diagnostic with node-agnostic cache dir (consumed by Plan B verification)"
      contains: ".jax_cache_gpu/shared"
    - path: "scripts/08_run_power_iteration.py"
      provides: "Benchmark runner whose in-process warm-up now uses production-shape N=max(n_per_group_grid) rather than N=2"
      contains: "max(power_config.n_per_group_grid)"
  key_links:
    - from: "cluster/14_benchmark_gpu.slurm JAX_COMPILATION_CACHE_DIR"
      to: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"
      via: "shared Lustre mount — all GPU SLURM jobs read/write the same cache dir"
      pattern: ".jax_cache_gpu/shared"
    - from: "scripts/08_run_power_iteration.py _run_benchmark Phase 1 warm-up"
      to: "scripts/08_run_power_iteration.py _run_benchmark Phase 2 full-iteration"
      via: "shared in-process tracing cache; same N → same HLO scan body → warm call hits trace cache"
      pattern: "max(power_config.n_per_group_grid)"
---

<objective>
Repo-wide cluster infrastructure patch implementing Phase 21 VERDICT §(c):

1. Drop ${SLURMD_NODENAME} from every JAX_COMPILATION_CACHE_DIR path in cluster/*.slurm so the persistent compilation cache is reused cross-node via the shared Lustre scratch mount (no scheduler pinning required).
2. Replace the N=2/group Phase-1 warm-up inside scripts/08_run_power_iteration.py::_run_benchmark with a production-shape cohort so the in-process warm call exercises the exact HLO the Phase-2 measurement needs (VERDICT §(c) secondary patch — Jaccard 0.180 between N=2 and N=50 scan-bodies per 21-06 §c).

Both changes land in a SINGLE atomic commit so `git revert <hash>` rolls back the repo-wide infrastructure change cleanly (CONTEXT rollback-unit directive).

This plan is autonomous: it only edits files and commits. It does not submit any SLURM jobs. Verification of the fix (the P=50 next_proc_warm / cold ratio gate) lives in Plan 14.1-07b, which depends on this plan being committed and pushed.

Purpose: VERDICT §(a) names per-node cache scope vs SLURM scheduler mismatch as the dominant bottleneck producing the 1.1× warm-vs-cold speedup in job-54899271. This plan operationalises the VERDICT fix across the cluster surface so 14.1-03 can finally get a real benchmark write of results/power/benchmark_batched.json.

Output: Nine SLURM scripts repointed to a single shared cache dir + one Python script with its Phase-1 warm-up shape upgraded to production, committed atomically and pushed to origin/main.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14.1-07-CONTEXT.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md
@cluster/14_benchmark_gpu.slurm
@scripts/08_run_power_iteration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface prior single-subprocess attempt lineage (cheap lineage probe)</name>
  <files>(no file edits; read-only probe to inform commit message)</files>
  <action>
  CONTEXT flags a user-stated prior attempt: "single-subprocess restructure was attempted previously and had 'similar problems'". Give future-Claude a link so we never re-attempt that failed path.

  Step 1 — Grep the planning tree for any prior SUMMARY/STATE mention of a single-subprocess benchmark attempt or an earlier cache-dir patch:

    grep -rnI "single-subprocess\|single subprocess\|one subprocess\|cache_dir\|JAX_COMPILATION_CACHE_DIR" .planning/phases/ .planning/STATE.md | grep -v 14.1-07 | head -40

  Step 2 — Also grep for any documented prior warmup refactor attempts:

    grep -rnI "warm.?up refactor\|warmup refactor\|in-process warmup\|N=2 warm" .planning/ | head -20

  Step 3 — Record the findings inline in this task's on-disk log (append to the commit message preamble later; no standalone file). If the grep surfaces a specific SUMMARY file that documents a prior failed attempt, capture its path and a one-line description for use in Task 3's commit message. If nothing surfaces, record "no prior lineage found — this is the first cluster cache-scope patch in .planning/ history" and proceed.

  Note: This is read-only investigation — do NOT edit any files. It is intentionally boxed as a task so the gap-closure executor does it BEFORE the atomic patch commit, not after.
  </action>
  <verify>
  Executor has written (to the running chat, not a file) either:
    (a) a bullet list of path:line references to prior attempts, or
    (b) the phrase "no prior lineage found" as the bottom of their Task 1 output.
  </verify>
  <done>
  Prior-attempt lineage either surfaced with paths or conclusively shown to be absent; result is ready for inclusion in Task 3's commit message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch all nine SLURM scripts + _run_benchmark Phase 1 block (atomic edits, do not commit yet)</name>
  <files>
    cluster/14_benchmark_gpu.slurm
    cluster/08_power_sweep.slurm
    cluster/16_smoke_test_cpu.slurm
    cluster/16_smoke_test_gpu.slurm
    cluster/18_smoke_patrl_cpu.slurm
    cluster/21_diagnose_laplace.slurm
    cluster/21_diagnose_nuts.slurm
    cluster/21_diagnose_pscan.slurm
    cluster/patrl_smoke.slurm
    scripts/08_run_power_iteration.py
  </files>
  <action>
  SCOPE NOTE: `grep -l SLURMD_NODENAME cluster/*.slurm` returns 17 files (per CONTEXT). Of those 17, only NINE have SLURMD_NODENAME in the cache-dir path itself (verified via grep pattern `JAX_COMPILATION_CACHE_DIR=.*SLURMD_NODENAME`). The other 8 (01_diagnostic_gpu, 04_fit_mcmc_gpu, 05_validation_gpu, 06_analysis, 09_power_postprocess, 14_valid03_cpu, 14_valid03_gpu, 99_push_results) use SLURMD_NODENAME only in echo/banner `Node: ...` lines — these banners are operator visibility for fallback debugging and MUST be preserved unchanged. Do NOT edit the banner-only files.

  Step 1 — Nine SLURM cache-dir edits. In each of the following files, find the line:

      export JAX_COMPILATION_CACHE_DIR="/scratch/${_PROJECT}/${USER}/.jax_cache_{gpu|cpu}[/subdir]/${SLURMD_NODENAME:-local}"

  and replace the trailing `/${SLURMD_NODENAME:-local}` segment with `/shared` (exact subdir name chosen: `shared`; rationale: explicit, self-documenting, distinct from any existing per-node subdir so we know the patched run used the new path). Preserve all other path components exactly as they were.

  Per-file expected patches:

    - cluster/14_benchmark_gpu.slurm:99
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"

    - cluster/08_power_sweep.slurm:124
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"

    - cluster/16_smoke_test_cpu.slurm:63
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_cpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_cpu/shared"

    - cluster/16_smoke_test_gpu.slurm:120
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"

    - cluster/18_smoke_patrl_cpu.slurm:61
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_cpu/patrl/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_cpu/patrl/shared"

    - cluster/21_diagnose_laplace.slurm:116
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"

    - cluster/21_diagnose_nuts.slurm:134
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"
      ALSO update the comment on line 33-34 that explicitly names the old path formula so the comment doesn't lie.

    - cluster/21_diagnose_pscan.slurm:157
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_gpu/shared"
      ALSO update the header comment near line 28 that names the old path formula.

    - cluster/patrl_smoke.slurm:56
        before: "/scratch/${_PROJECT}/${USER}/.jax_cache_cpu/patrl/${SLURMD_NODENAME:-local}"
        after:  "/scratch/${_PROJECT}/${USER}/.jax_cache_cpu/patrl/shared"

  For cluster/14_benchmark_gpu.slurm specifically, also update the comment on line 98 from "(node-specific, persistent across jobs for BENCH-05)" to "(shared across nodes via Lustre scratch; cross-SLURM-job persistence — Phase 21 VERDICT §c)".

  DO NOT modify any echo/banner `Node: ${SLURMD_NODENAME:-$(hostname)}` lines — they display which node SLURM chose, which stays useful diagnostically.

  Step 2 — Python _run_benchmark Phase-1 warm-up upgrade in scripts/08_run_power_iteration.py.

  Target range: lines 1058–1144 in _run_benchmark (the "--- Phase 1: JAX compilation cache test (BENCH-05) ---" block).

  Edit:
    - Line 1060 prompt: change "Building tiny cohort for JIT warm-up (N=2/group, 2 chains, 10 draws)..." to "Building production-shape cohort for JIT warm-up (N=max(n_per_group_grid), args.fit_chains chains, args.fit_tune tune / args.fit_draws draws)..."
    - Line 1062–1063: replace `d_bench = power_config.effect_size_grid[0]` + `cfg_tiny = make_power_config(base_config, 2, d_bench, 99999)` with:
        d_bench = power_config.effect_size_grid[0]
        max_n_warm = max(power_config.n_per_group_grid)
        cfg_tiny = make_power_config(base_config, max_n_warm, d_bench, 99999)
    - Rename local vars `cfg_tiny`, `sim_tiny` → `cfg_warm`, `sim_warm` throughout the Phase-1 block for clarity (keeps "tiny" from lying about shape).
    - Lines 1070–1079 (cold call) and 1112–1122 (warm call): update the fit_batch_hierarchical() arguments so `n_chains=args.fit_chains`, `n_draws=args.fit_draws`, `n_tune=args.fit_tune` instead of the previous `n_chains=2, n_draws=10, n_tune=10`. Keep everything else unchanged (random_seed, target_accept, progressbar, warmup_params reuse logic via Patch C).

  Rationale (embed as an updated comment above the Phase-1 block, replacing the "N=2 warm-up" comment): "Phase 21 VERDICT §c secondary patch (14.1-07): the former N=2/group warm-up exercised a structurally different HLO scan body (hash 2f25aed94588c656, 4,771,952 B) from the Phase-2 N=max_n/group measurement (hash 62fbc95475cb0a32, 4,786,267 B; Jaccard 0.180 per 21-06 §c). The tracing cache could not bridge the shape gap so Phase 1 contributed compile time without accelerating Phase 2. Upgrading Phase 1 to production N means the in-process tracing cache hits cleanly and Phase 2 runs warm-JIT-only."

  Do NOT change Phase 2 (the full run_sbf_iteration call starting around line 1155). It already uses max(n_per_group_grid).

  Step 3 — Validate the edits locally without running cluster jobs:
    - `grep -c 'SLURMD_NODENAME' cluster/*.slurm` across all 17 files: expect the 8 banner-only files each to have ≥ 1 match and the 9 patched files to still have their echo/banner lines but 0 matches in cache-dir export lines.
    - `grep -n 'JAX_COMPILATION_CACHE_DIR=.*SLURMD_NODENAME' cluster/*.slurm` returns nothing (zero lines).
    - `grep -n 'JAX_COMPILATION_CACHE_DIR=.*shared"' cluster/*.slurm` returns exactly 9 lines (one per patched script).
    - Python import sanity: `python -c "import ast; ast.parse(open('scripts/08_run_power_iteration.py').read())"` returns exit 0.
    - `grep -n 'max(power_config.n_per_group_grid)' scripts/08_run_power_iteration.py` returns ≥ 2 matches (Phase 1 new usage + existing Phase 2 usage).

  DO NOT COMMIT. Commit lands in Task 3 as a single atomic unit.
  </action>
  <verify>
  1. `grep -n 'JAX_COMPILATION_CACHE_DIR=.*SLURMD_NODENAME' cluster/*.slurm` returns zero lines.
  2. `grep -c 'JAX_COMPILATION_CACHE_DIR=.*shared' cluster/*.slurm | grep -v ':0$' | wc -l` equals 9.
  3. `python -c "import ast; ast.parse(open('scripts/08_run_power_iteration.py').read())"` exits 0.
  4. `grep -n 'max_n_warm' scripts/08_run_power_iteration.py` shows the new Phase-1 cohort-shape assignment inside _run_benchmark.
  5. `git diff --stat cluster/*.slurm scripts/08_run_power_iteration.py` shows 10 files touched, ~25 insertions/deletions order of magnitude (sanity).
  </verify>
  <done>
  Nine SLURM scripts point to `/shared`; _run_benchmark Phase-1 uses production N; Python parses; nothing committed yet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit + push to origin/main (rollback unit)</name>
  <files>
    cluster/14_benchmark_gpu.slurm
    cluster/08_power_sweep.slurm
    cluster/16_smoke_test_cpu.slurm
    cluster/16_smoke_test_gpu.slurm
    cluster/18_smoke_patrl_cpu.slurm
    cluster/21_diagnose_laplace.slurm
    cluster/21_diagnose_nuts.slurm
    cluster/21_diagnose_pscan.slurm
    cluster/patrl_smoke.slurm
    scripts/08_run_power_iteration.py
  </files>
  <action>
  Step 1 — Stage only the files this plan owns:

    git add cluster/14_benchmark_gpu.slurm cluster/08_power_sweep.slurm \
            cluster/16_smoke_test_cpu.slurm cluster/16_smoke_test_gpu.slurm \
            cluster/18_smoke_patrl_cpu.slurm cluster/21_diagnose_laplace.slurm \
            cluster/21_diagnose_nuts.slurm cluster/21_diagnose_pscan.slurm \
            cluster/patrl_smoke.slurm scripts/08_run_power_iteration.py

  Step 2 — Create a single atomic commit (per CONTEXT: "every SLURM-script modification lands in a single atomic commit so `git revert <hash>` rolls back the repo-wide change cleanly"). Use a HEREDOC, fix(14.1-07): prefix per phase convention, NO Co-Authored-By (global CLAUDE.md directive):

    git commit -m "$(cat <<'EOF'
fix(14.1-07): drop per-node cache suffix + upgrade Phase-1 warm-up to production N

Phase 21 VERDICT §(c) fix. Nine SLURM scripts stop routing JAX_COMPILATION_CACHE_DIR
through ${SLURMD_NODENAME:-local} and instead share /scratch/.../jax_cache_{gpu|cpu}[/subdir]/shared
via the Lustre mount; cross-SLURM-job cache reuse no longer requires node-affinity.
scripts/08_run_power_iteration.py _run_benchmark Phase-1 warm-up cohort switches from
N=2/group (HLO scan body hash 2f25aed94588c656) to production N=max(n_per_group_grid)
(hash 62fbc95475cb0a32) so the in-process tracing cache actually hits for Phase-2
(Jaccard 0.180 between N=2 and N=50 bodies per 21-06 §c).

Atomic revert unit per CONTEXT.md rollback directive.

Unblocks 14.1-07b diagnostic verification (pscan P=50 next/cold ratio ≥ 3.0×).
EOF
)"

  Step 3 — Push immediately (CLAUDE.md workflow rule: "Always push after committing — this repo is worked on from both a local machine and the M3 cluster"):

    git push origin main

  If push fails due to non-fast-forward (someone pushed in between), pull-rebase and retry ONCE:

    git pull --rebase origin main && git push origin main

  If it still fails, stop and surface the error — do not force-push.
  </action>
  <verify>
  1. `git log --oneline -1` shows a commit starting with `fix(14.1-07):`.
  2. `git log --name-only -1` lists exactly the 10 files in files_modified.
  3. `git status` reports "working tree clean" modulo the usual untracked scratch/verification files.
  4. `git rev-parse origin/main` equals `git rev-parse HEAD` (push succeeded).
  5. `git log -1 --format='%s'` does NOT contain "Co-Authored-By".
  </verify>
  <done>
  Single atomic commit on origin/main carrying all ten infrastructure edits; ready for M3 git pull and Plan 14.1-07b diagnostic submission.
  </done>
</task>

</tasks>

<out_of_scope>
- Do NOT touch `src/prl_hgf/fitting/hierarchical_patrl.py::_build_patrl_log_posterior` closure — VERDICT §(c) "explicitly do NOT" list; the closure-over-data hypothesis is FALSIFIED by 21-06 HLO byte-identity evidence.
- Do NOT modify `apply_decision_gate` thresholds — STATE #105 defers until after 14.1-03 lands a real benchmark JSON.
- Do NOT edit `results/power/benchmark_batched.json` — 14.1-03 owns that artifact.
- Do NOT reshape 14.1-03's benchmark CLI surface (--benchmark flag, --fit-chains/draws/tune defaults).
- Do NOT submit any SLURM jobs in this plan — Plan 14.1-07b owns the diagnostic verification submission.
- Do NOT edit the 8 banner-only SLURM scripts (01_diagnostic_gpu, 04_fit_mcmc_gpu, 05_validation_gpu, 06_analysis, 09_power_postprocess, 14_valid03_cpu, 14_valid03_gpu, 99_push_results) — they reference SLURMD_NODENAME only for operator visibility in `Node: ...` echo lines, not in the cache path.
</out_of_scope>

<verification>
1. All nine cache-dir lines are node-agnostic; repo-wide grep invariant holds.
2. Python AST parses; Phase-1 warm-up Python block uses production N; commit is atomic.
3. Nothing outside files_modified is touched (important because other 14.1 plans may still be in-flight on the local/cluster).
</verification>

<success_criteria>
- Single atomic commit `fix(14.1-07): ...` is on origin/main and references the VERDICT §(c) rollback unit.
- Plan 14.1-07b can run `git pull origin main` on M3 and then `bash cluster/21_submit_pscan_chain.sh` directly; no further file edits required.
- No production `src/prl_hgf/` code changed; `git diff --stat src/` for this commit shows 0 lines.
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-07a-SUMMARY.md` with:
- Which 9 SLURM scripts were patched (explicit list)
- Why the 8 banner-only SLURM scripts were intentionally NOT patched (operator visibility preserved)
- Commit SHA + push status
- Any lineage notes from Task 1 (prior-attempt pointers or "none found")
- Explicit handoff signal to Plan 14.1-07b: "M3 ready for `git pull origin main` then `bash cluster/21_submit_pscan_chain.sh`"
</output>

---
phase: 14-integration-gpu-benchmark
plan: "14.1-07b"
type: execute
wave: 2
depends_on: ["14.1-07a"]
files_modified:
  - .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
  - .planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv
  - cluster/14_benchmark_gpu.slurm
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Step 1 (diagnostic) has executed: M3 ran `bash cluster/21_submit_pscan_chain.sh` after pulling the 14.1-07a commit; all 6 pscan jobs finished and auto-pushed their logs"
    - "jit_scaling_sweep.csv has post-patch rows appended for P ∈ {5, 20, 50} covering both cold and next-proc-warm columns; pre-patch rows preserved (never overwritten) for audit"
    - "At P=50 the ratio `next_proc_warm_s / cold_jit_s` ≤ 1/3.0 (equivalently `cold_jit_s / next_proc_warm_s ≥ 3.0×`), i.e. next_proc_warm_s ≤ 75s baseline permitting; recorded verbatim in VERIFICATION.md"
    - "At P=5 the in-process `same_proc_warm_s / cold_jit_s` speedup stays ≥ 2.0× (regression guard — no in-process tracing-cache regression from the infrastructure change)"
    - "At P=50 cold_jit_s ≤ 300s (secondary sanity check — no inadvertent slow-node routing)"
    - "Lustre contention probe: `grep -iE 'flock|cache entry version mismatch|cache lock|lustre' pscan_P*.log` output captured in VERIFICATION.md (zero hits is the expected/pass outcome)"
    - "If the primary gate PASSES: active_strategy=`shared-dir`, VERIFICATION.md status=`complete`, no further SLURM edits"
    - "If the primary gate FAILS: active_strategy=`nodelist-fallback` — cluster/14_benchmark_gpu.slurm gets a `#SBATCH --nodelist=<pinned>` directive (other 8 shared-cache SLURM scripts remain at /shared), fallback is committed separately, VERIFICATION.md documents the fallback trigger with the failing numbers"
  artifacts:
    - path: ".planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md"
      provides: "Quantitative Step-1 diagnostic results with PASS/FAIL verdict + active strategy"
      contains: "next_proc_warm_s"
    - path: ".planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv"
      provides: "Post-patch pscan rows appended for audit trail"
      contains: "completed"
  key_links:
    - from: ".planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md"
      to: ".planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv"
      via: "quantitative numbers cite specific CSV rows"
      pattern: "jit_scaling_sweep.csv"
    - from: ".planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md"
      to: ".planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md"
      via: "gate criteria are the quantitative invariants from VERDICT §e"
      pattern: "next_proc_warm_s / cold_jit_s"
---

<objective>
Verify Phase 14.1-07a's cluster-infrastructure patch actually closes the Phase 21 bottleneck by re-running the Phase 21 P-sweep chain on M3 and checking the VERDICT §e quantitative invariants. This plan is NOT autonomous: it requires the user to run `bash cluster/21_submit_pscan_chain.sh` on M3 (~40 min cluster wall), then Claude reads the auto-pushed CSV + logs and makes a PASS/FAIL judgment.

Branch:
- If PASS (next_proc_warm_s / cold_jit_s ≥ 3.0× at P=50; P=5 same_proc_warm speedup ≥ 2.0×): VERIFICATION.md records `active_strategy=shared-dir, status=complete`. Plan closes. 14.1-07c (hand-off memo) takes over.
- If FAIL: apply the CONTEXT-defined fallback — add `#SBATCH --nodelist=<pinned>` to cluster/14_benchmark_gpu.slurm ONLY (leave other 8 shared-cache scripts unchanged), commit separately, re-run the pscan chain, and re-verify. Record the failure → fallback → re-verify sequence in VERIFICATION.md.

Purpose: Without a quantitative Step-1 gate, 14.1-03 is still at risk of timing out at 8h walltime on the same 1.1× cold↔warm paradox. This plan is the cheap cluster-time gate that prevents expensive benchmark-time failure.

Output: `.planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md` with Step-1 numbers + PASS/FAIL verdict + active strategy; updated pscan CSV; (conditionally) a fallback commit on cluster/14_benchmark_gpu.slurm.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14.1-07-CONTEXT.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/VERDICT.md
@.planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv
@cluster/21_submit_pscan_chain.sh
@cluster/21_diagnose_pscan.slurm
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: User runs pscan_chain on M3 after pulling 14.1-07a</name>
  <what-built>
  Plan 14.1-07a committed the repo-wide `/shared` cache-dir switch + _run_benchmark Phase-1 production-shape warm-up. The pscan chain re-submission is how we verify that switch actually delivers the ≥ 3.0× cold↔warm speedup at P=50 predicted by VERDICT §e.
  </what-built>
  <how-to-verify>
  Exact commands to run, copied from VERDICT §e:

    # On M3, from the repo root:
    cd ~/psilocybin_prl_analyses
    git pull origin main

    # Sanity: confirm 14.1-07a landed
    grep -c 'JAX_COMPILATION_CACHE_DIR=.*SLURMD_NODENAME' cluster/*.slurm | grep -v ':0$'
    # Expect: zero lines of output (all 17 scripts now either don't use it or route via /shared).

    grep -c 'JAX_COMPILATION_CACHE_DIR=.*/shared' cluster/*.slurm | grep -v ':0$' | wc -l
    # Expect: 9

    # Submit the 6-job P-sweep chain. Each P pair is cold→warm afterok.
    bash cluster/21_submit_pscan_chain.sh
    # Budget: ~40 min wall (per-pair 30-min wall cap; pairs run in parallel).

    # The 21_diagnose_pscan.slurm scripts auto-push their log artifacts +
    # jit_scaling_sweep.csv updates. Wait for all 6 jobs to complete:
    squeue -u $USER --name=prl_pscan_P5_cold,prl_pscan_P5_warm,prl_pscan_P20_cold,prl_pscan_P20_warm,prl_pscan_P50_cold,prl_pscan_P50_warm
    # When empty, all jobs done. Watch commits:
    watch -n 30 'git fetch origin main && git log origin/main --oneline -10'

  When all 6 jobs report completion (either in-queue-empty or the final commit lands), the user should respond with:
    "chain complete" + the 6 job IDs (or "failed at P=XX" if one job timed out).
  </how-to-verify>
  <resume-signal>
  User responds with "chain complete" (or "failed at P=XX") plus the 6 job IDs. Claude then proceeds to Task 2.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pull M3 commits locally, read CSV, evaluate PASS/FAIL gates, write VERIFICATION.md</name>
  <files>
    .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
  </files>
  <action>
  Step 1 — Sync local with the cluster's auto-pushed commits:

    git pull origin main

  Step 2 — Read the updated pscan CSV:

    cat .planning/phases/21-benchmark-bottleneck-diagnosis/jit_scaling_sweep.csv

  Identify the NEW rows (post-patch — submitted after the 14.1-07a fix SHA). Preserve the pre-patch rows for audit. The CSV currently has 8 rows (3 P=5, 3 P=20, 2 P=50); after Plan 14.1-07b Task 1's chain, expect 8 new rows (6 from the chain's cold+warm, possibly plus 2 same_proc reruns depending on 21_diagnose_pscan.slurm's behaviour — check the script).

  Step 3 — Probe for Lustre contention in the auto-pushed logs:

    ls .planning/phases/21-benchmark-bottleneck-diagnosis/pscan_P*.log | tail -6
    grep -iEn 'flock|cache entry version mismatch|cache lock|lustre|concurrent' .planning/phases/21-benchmark-bottleneck-diagnosis/pscan_P*.log | head -20

  Record the full grep output (even if zero lines) verbatim in VERIFICATION.md's "Lustre contention probe" subsection. Zero lines is the expected pass outcome.

  Step 4 — Evaluate gates using the NEW CSV rows only.

  Gate definitions (verbatim from VERDICT §e + CONTEXT specifics):
    Primary gate:
      next_proc_warm_s / cold_jit_s ≤ 1/3.0 at P=50
      equivalently cold_jit_s / next_proc_warm_s ≥ 3.0×
      target: next_proc_warm_s ≤ 75s at P=50
    Secondary gate:
      cold_jit_s ≤ 300s at P=50 (slow-node guard)
    Regression gate:
      same_proc_warm_s / cold_jit_s ≤ 0.5 at P=5 (i.e. ≥ 2.0× in-process speedup)

  ALL THREE must pass for active_strategy=shared-dir. Any single failure triggers the fallback branch in Task 3.

  Step 5 — Write .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md with this structure (YAML frontmatter + markdown body):

    ---
    phase: 14-integration-gpu-benchmark
    plan: "14.1-07"
    step: 1-diagnostic
    verified: <today's ISO date>
    status: <complete | fallback_triggered | gate_failed>
    active_strategy: <shared-dir | nodelist-fallback>
    ---

    # Phase 14.1-07 Step-1 Diagnostic Verification

    ## Gate Results (VERDICT §e quantitative invariants)

    | Gate | Target | Observed | Verdict |
    |------|--------|----------|---------|
    | Primary: cold/next_proc_warm @ P=50 | ≥ 3.0× | <observed>× | <PASS|FAIL> |
    | Secondary: cold_jit_s @ P=50 | ≤ 300s | <observed>s | <PASS|FAIL> |
    | Regression: same_proc_warm/cold @ P=5 | ≥ 2.0× | <observed>× | <PASS|FAIL> |

    ## New CSV Rows (post-patch — commit <14.1-07a SHA>)

    <table of new jit_scaling_sweep.csv rows cited by line number>

    ## Lustre Contention Probe

    Grep over pscan_P{5,20,50}_{cold,warm}.log for flock / cache entry version mismatch / lustre / concurrent:

    ```
    <verbatim grep output, zero lines expected>
    ```

    ## Active Strategy

    <one paragraph: shared-dir (if all gates PASS) OR nodelist-fallback (if any gate FAILED); if fallback, cross-reference the fallback commit SHA from Task 3>

    ## Evidence Commits

    - 14.1-07a patch: <SHA>
    - pscan cold+warm auto-pushes: <SHAs from git log>
    - (if fallback) cluster/14_benchmark_gpu.slurm nodelist patch: <SHA>

    ## Phase 14.1-03 Unblock Signal

    <paragraph: PASS → 14.1-03 can now submit sbatch cluster/14_benchmark_gpu.slurm unchanged; FAIL-then-fallback-PASS → 14.1-03 uses the patched nodelist-pinned script; FAIL-and-fallback-also-FAIL → escalate, do NOT unblock 14.1-03>

  Step 6 — Commit the VERIFICATION doc:

    git add .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
    git commit -m "docs(14.1-07): write Step-1 diagnostic verification (active_strategy=<shared-dir|nodelist-fallback>)"
    git push origin main

  No Co-Authored-By.
  </action>
  <verify>
  1. `grep -E '^status:' .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md` shows one of `complete`, `fallback_triggered`, `gate_failed`.
  2. `grep -E '^active_strategy:' .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md` shows one of `shared-dir`, `nodelist-fallback`.
  3. VERIFICATION.md cites at least one specific CSV line and at least one specific pscan_P*.log file.
  4. `git log --oneline -- .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md` shows the docs(14.1-07): commit.
  5. `git rev-parse origin/main == git rev-parse HEAD` (push succeeded).
  </verify>
  <done>
  VERIFICATION.md committed with Step-1 numbers + PASS/FAIL + active strategy. If PASS, plan falls through to close. If FAIL, plan routes to Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3 (CONDITIONAL — only if Task 2 verdict is FAIL): Apply nodelist fallback, resubmit, re-verify</name>
  <files>
    cluster/14_benchmark_gpu.slurm
    .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
  </files>
  <action>
  GATE: only execute this task if Task 2's VERIFICATION.md has `status: gate_failed` or `fallback_triggered`. If Task 2 wrote `status: complete`, SKIP this task entirely and proceed to the plan's close-out commit.

  Step 1 — Pick a pinned A100 node. From the Task 1 completed job logs (they printed Node banners), pick the node whose cold_jit_s at P=50 was ≤ 300s (the fastest observed node is the safest pin). If all nodes were slow, default to `m3a118` (the node job-54899271 ran on — its timing is a known baseline).

  Step 2 — Edit cluster/14_benchmark_gpu.slurm ONLY (do NOT add --nodelist to other scripts). Insert this line immediately AFTER the `#SBATCH --partition=gpu` directive (around line 23) and BEFORE `#SBATCH --cpus-per-task=4`:

    #SBATCH --nodelist=<pinned_node>          # Phase 14.1-07 fallback: shared-cache gate failed; pin this benchmark run to a single node so next_proc cache hits reliably. Other GPU SLURM scripts keep the /shared cache dir — this is a targeted fallback, not a repo-wide revert.

  Step 3 — Commit the fallback (SEPARATE from 14.1-07a so `git revert` still works cleanly on the main patch if the fallback itself causes problems):

    git add cluster/14_benchmark_gpu.slurm
    git commit -m "fix(14.1-07): fallback --nodelist=<pinned> for 14_benchmark_gpu.slurm"
    git push origin main

  Step 4 — Have the user re-run the pscan chain on M3 (same command as Task 1). Checkpoint: emit a human-action block asking:

    "Shared-cache gate failed (see 14.1-07-VERIFICATION.md Task 2). Pushed nodelist fallback commit <SHA>. Please re-run on M3:
      git pull origin main
      bash cluster/21_submit_pscan_chain.sh
    Respond 'fallback chain complete' when done."

  Step 5 — After the user confirms, re-run Task 2's Step 1–4 on the new CSV rows. Update 14.1-07-VERIFICATION.md (append a "Fallback Re-Verification" section with the new gate numbers; do NOT overwrite the original Step-1-FAIL section — keep the failure for audit). Update `status` to either `complete` (if fallback PASSES all gates) or `gate_failed` (if fallback ALSO fails — escalate). Update `active_strategy` to `nodelist-fallback` on PASS, or leave at `shared-dir` with an explicit failure note on escalate-case.

  Step 6 — Commit the updated VERIFICATION:

    git add .planning/phases/14-integration-gpu-benchmark/14.1-07-VERIFICATION.md
    git commit -m "docs(14.1-07): fallback re-verification — active_strategy=nodelist-fallback"
    git push origin main

  If the fallback ALSO fails (gate_failed after nodelist pin), STOP — do not write a SUMMARY, do not attempt 14.1-03. Surface the escalation to the user: "Both shared-dir and nodelist strategies failed at P=50. Residual concern VERDICT §(g) Track (i) JAX #22281 tracing paradox is now primary. Recommend opening a new diagnostic plan before attempting 14.1-03."
  </action>
  <verify>
  IF Task 2 verdict was PASS:
    Task 3 is SKIPPED. `git log --oneline` since this plan started has exactly 1 commit (the VERIFICATION doc from Task 2).

  IF Task 2 verdict was FAIL AND fallback runs:
    1. `grep -n '#SBATCH --nodelist=' cluster/14_benchmark_gpu.slurm` returns exactly 1 line.
    2. `grep -n '#SBATCH --nodelist=' cluster/*.slurm | wc -l` equals 1 (only benchmark script, no other scripts pinned).
    3. VERIFICATION.md has both a "Step-1 Diagnostic" section (FAIL) and a "Fallback Re-Verification" section (PASS or escalate).
    4. `git log --oneline -5` shows both the fix(14.1-07): nodelist commit and the final docs(14.1-07): fallback re-verification commit.
  </verify>
  <done>
  Either (a) skipped (shared-dir gate passed) or (b) fallback applied, chain re-run, and VERIFICATION.md updated with both phases. Plan 14.1-07b closes either way unless the escalation branch fires.
  </done>
</task>

</tasks>

<out_of_scope>
- Do NOT submit `sbatch cluster/14_benchmark_gpu.slurm` — that is 14.1-03's deliverable; this plan stops at the pscan diagnostic chain. Plan 14.1-07c closes the loop with the hand-off memo to 14.1-03.
- Do NOT touch `src/prl_hgf/fitting/hierarchical_patrl.py` — VERDICT §(c) explicitly-not list.
- Do NOT modify `apply_decision_gate` thresholds.
- Do NOT edit `results/power/benchmark_batched.json`.
- Do NOT add `#SBATCH --nodelist=` to ANY script other than `cluster/14_benchmark_gpu.slurm` (even if fallback is triggered) — VERDICT §(c) is that the pinning is a surgical fallback for the specific benchmark script, not a repo-wide revert.
- Do NOT re-enter the 14.1-07a atomic commit — it has already landed; any further infrastructure change lives in its own separate commit per the rollback-unit directive.
</out_of_scope>

<verification>
1. Step-1 gates evaluated against VERDICT §e invariants (≥ 3.0× at P=50; ≤ 300s cold at P=50; ≥ 2.0× regression guard at P=5).
2. VERIFICATION.md cites quantitative evidence with CSV row references and log file paths.
3. Active strategy (shared-dir or nodelist-fallback) is unambiguously identified.
4. Phase 14.1-03 unblock signal is explicit.
</verification>

<success_criteria>
- 14.1-07-VERIFICATION.md exists with `status: complete` (PASS path) OR `status: complete` after nodelist fallback re-verify (FAIL-then-fallback path), each with quantitative numbers.
- Fallback case ONLY applies `--nodelist=` to cluster/14_benchmark_gpu.slurm (not other SLURM scripts).
- All commits end with `git push origin main`.
- No Co-Authored-By lines in commit messages.
- The single command Phase 14.1-03 must run to execute its benchmark is unambiguously identified in VERIFICATION.md (either `sbatch cluster/14_benchmark_gpu.slurm` unchanged, or `sbatch cluster/14_benchmark_gpu.slurm` with the node pin already in place).
</success_criteria>

<output>
After completion (regardless of pass/fail/fallback branch), create `.planning/phases/14-integration-gpu-benchmark/14.1-07b-SUMMARY.md` with:
- Final Step-1 gate verdict (pass / fallback-pass / escalated)
- Active strategy: `shared-dir` or `nodelist-fallback`
- Quantitative numbers: cold_jit_s @ P=50, next_proc_warm_s @ P=50, ratio, same_proc_warm speedup @ P=5
- Link to 14.1-07-VERIFICATION.md for the authoritative evidence
- Explicit handoff signal to Plan 14.1-07c (the hand-off / close-out memo to 14.1-03)
</output>
